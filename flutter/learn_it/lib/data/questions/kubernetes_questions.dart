import '../../models/question.dart';

final List<Question> kubernetesQuestions = [
  Question(
    questionText: 'What is the smallest deployable unit in Kubernetes?',
    options: ['Container', 'Pod', 'Service', 'Deployment'],
    correctAnswerIndex: 1,
    explanation: 'A Pod is the basic unit of deployment in Kubernetes; it can hold one or more containers. "Container" is what runs inside a Pod — Kubernetes does not schedule bare containers directly. "Service" is a networking abstraction that routes traffic to Pods — it is not a deployable compute unit. "Deployment" is a controller that manages a set of Pod replicas — it is a higher-level object that creates Pods.',
  ),
  Question(
    questionText: 'Which Kubernetes controller manages stateful applications with stable network identities?',
    options: ['Deployment', 'StatefulSet', 'DaemonSet', 'ReplicaSet'],
    correctAnswerIndex: 1,
    explanation: 'StatefulSet is used for stateful workloads that need stable identities and persistent storage. "Deployment" manages stateless applications with interchangeable Pods — Pods can be replaced without identity guarantees. "DaemonSet" ensures one Pod runs on every node — it is for node-level daemons, not stateful apps needing stable IDs. "ReplicaSet" maintains a desired number of Pod replicas — it does not provide stable network identities.',
  ),
  Question(
    questionText: 'Which Service type exposes a workload only inside the cluster?',
    options: ['ClusterIP', 'NodePort', 'LoadBalancer', 'ExternalName'],
    correctAnswerIndex: 0,
    explanation: 'ClusterIP exposes the Service on a cluster-internal IP address, accessible only within the cluster. "NodePort" exposes the Service on a static port on every node\'s IP — it is reachable externally via node IP:port. "LoadBalancer" provisions an external cloud load balancer, making the Service internet-accessible. "ExternalName" maps a Service to a DNS name outside the cluster — it is for external services, not internal-only.',
  ),
  Question(
    questionText: 'What is the main difference between a ConfigMap and a Secret?',
    options: [
      'ConfigMaps store non-sensitive config; Secrets store sensitive data.',
      'ConfigMaps are stored on disk; Secrets are stored in RAM only.',
      'ConfigMaps are managed by Helm; Secrets by the scheduler.',
      'There is no practical difference between them.',
    ],
    correctAnswerIndex: 0,
    explanation: 'ConfigMaps store non-sensitive configuration; Secrets store sensitive data like passwords and tokens. "ConfigMaps are stored on disk; Secrets are stored in RAM only" — both are stored in etcd by default; Secrets can optionally be stored in memory using RAM-backed tmpfs volumes. "ConfigMaps are managed by Helm; Secrets by the scheduler" — both are managed by the Kubernetes API server; Helm can manage both. They have a significant practical difference — Secrets are base64-encoded, can be encrypted at rest, and have stricter access controls.',
  ),
  Question(
    questionText: 'Which control-plane component assigns pods to nodes?',
    options: ['kube-apiserver', 'kube-controller-manager', 'kube-scheduler', 'etcd'],
    correctAnswerIndex: 2,
    explanation: 'The kube-scheduler watches for new pods with no node assignment and selects a node for them to run on. "kube-apiserver" is the front-end for the Kubernetes control plane — it validates and processes API requests but does not schedule. "kube-controller-manager" runs controllers like the ReplicaSet controller — it creates Pods but does not assign them to nodes. "etcd" is the distributed key-value store for cluster state — it stores scheduling decisions but does not make them.',
  ),
  Question(
    questionText: 'Which Kubernetes object partitions cluster resources for teams or projects?',
    options: ['Namespace', 'Node', 'Deployment', 'ServiceAccount'],
    correctAnswerIndex: 0,
    explanation: 'Namespaces provide virtual partitions of cluster resources for multi-tenancy. "Node" is a physical or virtual machine in the cluster — it is a compute resource, not a logical partition. "Deployment" manages application replicas — it is scoped to a namespace but does not partition resources. "ServiceAccount" provides an identity for pods — it is used for access control, not resource partitioning.',
  ),
  Question(
    questionText: 'What is Helm in the Kubernetes ecosystem?',
    options: [
      'A container runtime interface.',
      'A package manager for Kubernetes applications.',
      'A command-line tool for VM hardware configuration.',
      'A load balancer for HTTP traffic.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Helm packages and deploys Kubernetes applications using versioned charts. "A container runtime interface" describes CRI (Container Runtime Interface) — implemented by containerd or CRI-O. "A command-line tool for VM hardware configuration" — Helm manages Kubernetes resources, not VM hardware. "A load balancer for HTTP traffic" describes an Ingress controller or service mesh, not Helm.',
  ),
  Question(
    questionText: 'Which API object routes external HTTP and HTTPS traffic into cluster Services?',
    options: ['Service', 'Ingress', 'NetworkPolicy', 'Gateway'],
    correctAnswerIndex: 1,
    explanation: 'Ingress exposes HTTP(S) routes from outside the cluster and routes to backend Services. "Service" exposes Pods within or outside the cluster — without an Ingress, external access requires NodePort or LoadBalancer type. "NetworkPolicy" defines allow/deny rules for Pod network traffic — it does not route external HTTP traffic. "Gateway" is the newer Gateway API resource — while it can perform similar functions, Ingress is the traditional API for this.',
  ),
  Question(
    questionText: 'Which node agent ensures containers described by PodSpecs are running and healthy?',
    options: ['kubelet', 'kube-proxy', 'containerd', 'scheduler'],
    correctAnswerIndex: 0,
    explanation: 'The kubelet runs on each node and manages the pod lifecycle locally. "kube-proxy" maintains network rules for Service VIPs on each node — it handles networking, not container health. "containerd" is the container runtime that actually runs containers — it takes instructions from the kubelet. "scheduler" assigns Pods to nodes in the control plane — it does not run on worker nodes and does not monitor containers.',
  ),
  Question(
    questionText: 'Which workload resource ensures a copy of a pod runs on every (or selected) node?',
    options: ['Deployment', 'StatefulSet', 'DaemonSet', 'Job'],
    correctAnswerIndex: 2,
    explanation: 'DaemonSets run exactly one pod per selected node, useful for log agents and monitoring daemons. "Deployment" runs a fixed number of replicas across available nodes — it does not guarantee one pod per node. "StatefulSet" manages ordered, stateful pods with stable identities — it does not run on every node. "Job" runs pods to completion for batch tasks — it does not place pods on every node.',
  ),
  Question(
    questionText: 'What does a ReplicaSet do in Kubernetes?',
    options: [
      'Routes external traffic to pods.',
      'Ensures a specified number of pod replicas are running at any given time.',
      'Manages persistent storage for stateful apps.',
      'Assigns pods to nodes based on resource constraints.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A ReplicaSet maintains a stable set of replica pods, replacing any that fail or are deleted. "Routes external traffic to pods" is the role of a Service (ClusterIP, NodePort, LoadBalancer) or Ingress. "Manages persistent storage for stateful apps" is the role of StatefulSets and PersistentVolumes. "Assigns pods to nodes based on resource constraints" is the role of the kube-scheduler.',
  ),
  Question(
    questionText: 'What is the role of etcd in a Kubernetes cluster?',
    options: [
      'Manages networking rules for pod communication.',
      'Stores all cluster state and configuration data as a distributed key-value store.',
      'Schedules pods to appropriate nodes.',
      'Manages container images on each node.',
    ],
    correctAnswerIndex: 1,
    explanation: 'etcd is the backing store for all Kubernetes cluster data, including state and configuration. "Manages networking rules for pod communication" is the role of kube-proxy and CNI plugins. "Schedules pods to appropriate nodes" is the role of kube-scheduler. "Manages container images on each node" is the role of the container runtime (e.g., containerd) and image caching.',
  ),
  Question(
    questionText: 'What is a Kubernetes Deployment used for?',
    options: [
      'Provisioning persistent volumes for databases.',
      'Describing the desired state for stateless applications and managing rolling updates.',
      'Exposing applications to external traffic via DNS.',
      'Running one-off batch tasks to completion.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A Deployment manages stateless applications, rolling updates, and rollbacks using ReplicaSets. "Provisioning persistent volumes for databases" is done via PersistentVolumeClaims and StorageClasses. "Exposing applications to external traffic via DNS" is done by Services and Ingress resources. "Running one-off batch tasks to completion" is the role of the Job resource.',
  ),
  Question(
    questionText: 'What is a PersistentVolume (PV) in Kubernetes?',
    options: [
      'A temporary volume that lives only as long as the pod.',
      'A piece of storage in the cluster provisioned by an admin or dynamically, independent of pod lifecycle.',
      'A secret volume for storing credentials.',
      'A config volume for injecting environment variables.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A PersistentVolume is a cluster-level storage resource that exists independently of any pod. "A temporary volume that lives only as long as the pod" describes an emptyDir volume. "A secret volume for storing credentials" describes mounting a Secret as a volume — a subset of volume types, not a PV. "A config volume for injecting environment variables" describes mounting a ConfigMap as a volume or environment variable source.',
  ),
  Question(
    questionText: 'What is a PersistentVolumeClaim (PVC)?',
    options: [
      'A storage policy that defines how volumes are encrypted.',
      'A request for storage by a user or pod that binds to a matching PersistentVolume.',
      'A volume type specific to StatefulSets.',
      'A backup of a PersistentVolume.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A PVC is a user\'s request for storage; Kubernetes binds it to a matching PersistentVolume. "A storage policy that defines how volumes are encrypted" describes StorageClass parameters — not a PVC. "A volume type specific to StatefulSets" — while StatefulSets use volumeClaimTemplates (PVCs), PVCs themselves can be used by any pod. "A backup of a PersistentVolume" — Kubernetes does not have built-in PV backups; that requires external tools like Velero.',
  ),
  Question(
    questionText: 'What does a Kubernetes Job do?',
    options: [
      'Keeps a fixed number of pods running indefinitely.',
      'Runs a set of pods to completion, ensuring they succeed at least once.',
      'Schedules pods on every node in the cluster.',
      'Routes HTTP traffic based on URL paths.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A Job creates one or more pods and ensures they complete successfully. "Keeps a fixed number of pods running indefinitely" describes a Deployment or ReplicaSet. "Schedules pods on every node in the cluster" describes a DaemonSet. "Routes HTTP traffic based on URL paths" describes an Ingress resource or HTTPRoute in the Gateway API.',
  ),
  Question(
    questionText: 'What is a CronJob in Kubernetes?',
    options: [
      'A Job that scales up and down automatically.',
      'A scheduled Job that runs at specified times using a cron expression.',
      'A DaemonSet that runs maintenance tasks.',
      'A StatefulSet that rotates pod names on a schedule.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A CronJob creates Jobs on a recurring schedule defined by a cron expression. "A Job that scales up and down automatically" — Jobs run to completion; auto-scaling is a Deployment/HPA concept. "A DaemonSet that runs maintenance tasks" — DaemonSets run continuously on each node; a CronJob creates temporary Jobs on a schedule. "A StatefulSet that rotates pod names on a schedule" — StatefulSets maintain stable pod names; they do not rotate them.',
  ),
  Question(
    questionText: 'What is the purpose of a Kubernetes ServiceAccount?',
    options: [
      'To manage network traffic between services.',
      'To provide an identity for processes running in a pod to access the Kubernetes API.',
      'To assign CPU and memory limits to pods.',
      'To define health check endpoints for pods.',
    ],
    correctAnswerIndex: 1,
    explanation: 'ServiceAccounts provide an identity for pods, used for RBAC authorization with the Kubernetes API. "To manage network traffic between services" is done by Services, NetworkPolicies, and service meshes. "To assign CPU and memory limits to pods" is done via resource requests/limits in the pod spec, or LimitRange. "To define health check endpoints for pods" is done via liveness and readiness probes in the pod spec.',
  ),
  Question(
    questionText: 'What does RBAC (Role-Based Access Control) do in Kubernetes?',
    options: [
      'Limits CPU and memory usage per namespace.',
      'Regulates access to Kubernetes API resources based on roles assigned to users or service accounts.',
      'Controls network traffic between pods.',
      'Manages container image pull policies.',
    ],
    correctAnswerIndex: 1,
    explanation: 'RBAC uses Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings to grant permissions. "Limits CPU and memory usage per namespace" is done by ResourceQuotas and LimitRanges. "Controls network traffic between pods" is done by NetworkPolicies. "Manages container image pull policies" is done via imagePullPolicy in the pod spec — RBAC controls API access, not container behavior.',
  ),
  Question(
    questionText: 'What is a NetworkPolicy in Kubernetes?',
    options: [
      'A configuration for the DNS service in the cluster.',
      'A specification for how groups of pods can communicate with each other and external endpoints.',
      'A policy for scheduling pods across zones.',
      'A resource that limits bandwidth between nodes.',
    ],
    correctAnswerIndex: 1,
    explanation: 'NetworkPolicies define allow/deny rules for pod ingress and egress network traffic. "A configuration for the DNS service in the cluster" describes CoreDNS ConfigMaps — not NetworkPolicy. "A policy for scheduling pods across zones" describes topology spread constraints or node affinity rules. "A resource that limits bandwidth between nodes" — NetworkPolicies control access rules, not bandwidth limits.',
  ),
  Question(
    questionText: 'What is a Kubernetes Operator?',
    options: [
      'A user with cluster-admin privileges.',
      'A method of packaging, deploying, and managing a Kubernetes application using custom controllers.',
      'A tool for monitoring node resource usage.',
      'A plugin for extending the Kubernetes scheduler.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Operators encode operational knowledge into software using Custom Resources and custom controllers. "A user with cluster-admin privileges" is a human administrator — an Operator is software automation. "A tool for monitoring node resource usage" describes tools like metrics-server, Prometheus, or kubectl top. "A plugin for extending the Kubernetes scheduler" describes a scheduler plugin or framework, not an Operator.',
  ),
  Question(
    questionText: 'What is a Custom Resource Definition (CRD)?',
    options: [
      'A way to configure etcd replication.',
      'An extension to the Kubernetes API that lets you define custom objects.',
      'A method for modifying built-in pod templates.',
      'A type of Service that enables gRPC traffic.',
    ],
    correctAnswerIndex: 1,
    explanation: 'CRDs allow users to create new custom resource types which can be managed like built-in Kubernetes objects. "A way to configure etcd replication" — etcd replication is configured at the cluster infrastructure level, not through CRDs. "A method for modifying built-in pod templates" — built-in pod behavior can be altered by admission webhooks, not CRDs. "A type of Service that enables gRPC traffic" — Services support gRPC natively; gRPC routing is handled by Ingress or Gateway API.',
  ),
  Question(
    questionText: 'What does kubectl apply -f do?',
    options: [
      'Deletes resources defined in the file.',
      'Creates or updates resources declaratively based on the manifest file.',
      'Fetches logs from a pod in the current context.',
      'Applies a security patch to the cluster.',
    ],
    correctAnswerIndex: 1,
    explanation: 'kubectl apply -f creates or updates Kubernetes resources defined in a YAML or JSON manifest. "Deletes resources defined in the file" is done by kubectl delete -f. "Fetches logs from a pod" is done by kubectl logs <pod-name>. "Applies a security patch to the cluster" — cluster updates are done through the cluster lifecycle tool (e.g., kubeadm upgrade), not kubectl apply.',
  ),
  Question(
    questionText: 'What is a Kubernetes context?',
    options: [
      'A namespace inside a cluster.',
      'A set of access parameters (cluster, user, namespace) in the kubeconfig file.',
      'A security policy applied to a pod.',
      'A configuration for the API server.',
    ],
    correctAnswerIndex: 1,
    explanation: 'A context in kubeconfig specifies which cluster, user, and namespace kubectl commands target. "A namespace inside a cluster" — a namespace is a Kubernetes resource; a context can specify a default namespace but is broader. "A security policy applied to a pod" describes a PodSecurityContext or SecurityContext. "A configuration for the API server" — the API server is configured separately; contexts are client-side kubectl configuration.',
  ),
  Question(
    questionText: 'What is a liveness probe in Kubernetes?',
    options: [
      'A check that determines if a pod is ready to receive traffic.',
      'A check that determines if a container should be restarted because it is unhealthy.',
      'A check that measures CPU usage of a pod.',
      'A test that verifies network connectivity between pods.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Liveness probes detect stuck or dead containers and trigger a restart to recover. "A check that determines if a pod is ready to receive traffic" describes a readiness probe. "A check that measures CPU usage" — resource monitoring is done by metrics-server, not probes. "A test that verifies network connectivity between pods" — network testing uses tools like ping or curl; probes verify container health, not pod-to-pod connectivity.',
  ),
  Question(
    questionText: 'What is a readiness probe in Kubernetes?',
    options: [
      'A check that determines if a container should be restarted.',
      'A check that determines if a pod is ready to receive traffic from a Service.',
      'A scan for security vulnerabilities in container images.',
      'A test of cluster network connectivity.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Readiness probes prevent traffic from being sent to a pod until it is ready to serve requests. "A check that determines if a container should be restarted" describes a liveness probe. "A scan for security vulnerabilities in container images" describes image scanning tools like Trivy or Snyk. "A test of cluster network connectivity" — network testing uses dedicated tools; readiness probes test individual container startup status.',
  ),
  Question(
    questionText: 'What are resource requests and limits in Kubernetes?',
    options: [
      'The minimum and maximum number of pods a Deployment can run.',
      'The CPU and memory a container is guaranteed (request) and the maximum it can use (limit).',
      'The number of nodes a pod can be scheduled on.',
      'The storage size for a PersistentVolumeClaim.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Requests guarantee resources for scheduling; limits cap the maximum usage by a container. "The minimum and maximum number of pods a Deployment can run" — pod count is managed by HPA minReplicas/maxReplicas, not resource requests. "The number of nodes a pod can be scheduled on" is controlled by affinity, taints, or node selectors. "The storage size for a PersistentVolumeClaim" is set in the PVC\'s spec.resources.requests.storage field — a separate concept.',
  ),
  Question(
    questionText: 'What is a Horizontal Pod Autoscaler (HPA)?',
    options: [
      'A controller that vertically scales pod resources.',
      'A controller that automatically adjusts the number of pod replicas based on observed metrics.',
      'A tool for scaling nodes in the cluster.',
      'A resource that manages ingress traffic routing.',
    ],
    correctAnswerIndex: 1,
    explanation: 'HPA scales the number of replicas up or down based on CPU, memory, or custom metrics. "A controller that vertically scales pod resources" describes VPA (Vertical Pod Autoscaler) — it adjusts CPU/memory requests, not replica count. "A tool for scaling nodes in the cluster" describes the Cluster Autoscaler. "A resource that manages ingress traffic routing" describes an Ingress resource or Gateway API.',
  ),
  Question(
    questionText: 'What is a Vertical Pod Autoscaler (VPA)?',
    options: [
      'A controller that adds more replicas when load increases.',
      'A controller that recommends or automatically adjusts CPU and memory requests/limits for containers.',
      'A node-level autoscaler for the cluster.',
      'A policy for adjusting storage claims.',
    ],
    correctAnswerIndex: 1,
    explanation: 'VPA rightsizes pod resource requests and limits based on actual usage patterns. "A controller that adds more replicas when load increases" describes HPA (Horizontal Pod Autoscaler). "A node-level autoscaler for the cluster" describes the Cluster Autoscaler, which adds or removes nodes. "A policy for adjusting storage claims" — storage scaling is not handled by VPA; it is handled by StorageClasses with expandable volumes.',
  ),
  Question(
    questionText: 'What is the Cluster Autoscaler in Kubernetes?',
    options: [
      'A tool that adjusts pod replicas based on demand.',
      'A component that automatically adds or removes nodes based on pod scheduling needs.',
      'A service that scales the Kubernetes control plane.',
      'A resource that adjusts storage automatically.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Cluster Autoscaler adjusts the number of nodes, adding them when pods cannot be scheduled and removing underutilized ones. "A tool that adjusts pod replicas based on demand" describes HPA. "A service that scales the Kubernetes control plane" — control plane scaling is a cluster administration concern, not what Cluster Autoscaler does. "A resource that adjusts storage automatically" describes expandable PVCs using StorageClasses with volume expansion enabled.',
  ),
  Question(
    questionText: 'What does taint and toleration do in Kubernetes scheduling?',
    options: [
      'Encrypts communication between pods and nodes.',
      'Prevents pods from being scheduled on certain nodes unless they explicitly tolerate the taint.',
      'Limits bandwidth on specific network interfaces.',
      'Forces all pods in a namespace to run on specific node types.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Taints repel pods from nodes; tolerations allow pods to be scheduled on tainted nodes. "Encrypts communication between pods and nodes" — encryption is handled by network policies, mTLS, or service meshes like Istio. "Limits bandwidth on specific network interfaces" — bandwidth shaping is a network/CNI function, not scheduling. "Forces all pods in a namespace to run on specific node types" — this is better done with node selectors or affinity rules; taints/tolerations work the opposite way (repelling, not forcing).',
  ),
  Question(
    questionText: 'What is a node affinity rule in Kubernetes?',
    options: [
      'A rule that assigns network policies to nodes.',
      'A rule that constrains which nodes a pod can be scheduled on based on node labels.',
      'A rule that assigns storage to specific nodes.',
      'A rule that limits resource requests on a node.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Node affinity uses label selectors to express scheduling preferences or requirements for nodes. "A rule that assigns network policies to nodes" — NetworkPolicies apply to pods, not nodes. "A rule that assigns storage to specific nodes" — local storage affinity uses topology constraints in StorageClasses, not pod node affinity. "A rule that limits resource requests on a node" describes LimitRange or ResourceQuota — not node affinity.',
  ),
  Question(
    questionText: 'What is the purpose of an InitContainer in a pod?',
    options: [
      'To run alongside the main container to handle health checks.',
      'To run to completion before the main containers start, used for setup tasks.',
      'To provide storage for ephemeral data.',
      'To scale the main container automatically.',
    ],
    correctAnswerIndex: 1,
    explanation: 'InitContainers run sequentially to completion before app containers start, handling setup or dependency tasks. "To run alongside the main container for health checks" describes a sidecar pattern — InitContainers run before, not alongside, the main container. "To provide storage for ephemeral data" describes emptyDir volumes or ephemeral volumes. "To scale the main container automatically" describes HPA — InitContainers have no autoscaling function.',
  ),
  Question(
    questionText: 'What is a sidecar container pattern in Kubernetes?',
    options: [
      'A container that replaces the main container on failure.',
      'An additional container that runs alongside the main container to extend its functionality.',
      'A container that handles cluster DNS queries.',
      'A container that manages network policies.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Sidecar containers augment the main container\'s behavior (e.g., log shipping, proxying, monitoring). "A container that replaces the main container on failure" describes a restart policy — Kubernetes restarts the failed container itself, not a replacement. "A container that handles cluster DNS queries" describes CoreDNS, the cluster DNS service — not a sidecar. "A container that manages network policies" — NetworkPolicy enforcement is done by the CNI plugin at the node level, not by a sidecar.',
  ),
  Question(
    questionText: 'What is a Kubernetes Service Mesh?',
    options: [
      'A component for managing physical network cables between nodes.',
      'An infrastructure layer that handles service-to-service communication with features like mTLS, observability, and traffic control.',
      'A plugin for extending the Kubernetes scheduler.',
      'A tool for visualizing cluster networking topology.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Service meshes (like Istio, Linkerd) add mTLS, traffic management, and observability between microservices. "A component for managing physical network cables between nodes" — physical infrastructure is managed by the data center team, not a service mesh. "A plugin for extending the Kubernetes scheduler" describes a scheduler plugin/framework. "A tool for visualizing cluster networking topology" describes tools like Weave Scope or Kiali (which is part of Istio but a visualization tool).',
  ),
  Question(
    questionText: 'What is a PodDisruptionBudget (PDB)?',
    options: [
      'A policy that limits how many pods can be created in a namespace.',
      'A policy that limits how many pods of a service can be voluntarily disrupted simultaneously.',
      'A budget for managing cluster-level resource quotas.',
      'A limit on the number of nodes a pod can use.',
    ],
    correctAnswerIndex: 1,
    explanation: 'PDBs ensure a minimum number of pods remain available during voluntary disruptions like upgrades. "A policy that limits how many pods can be created in a namespace" describes ResourceQuota. "A budget for managing cluster-level resource quotas" also describes ResourceQuota — PDBs are about disruption tolerance, not resource limits. "A limit on the number of nodes a pod can use" — pods run on one node at a time; PDBs control disruption, not node scheduling.',
  ),
  Question(
    questionText: 'What is a ResourceQuota in Kubernetes?',
    options: [
      'A limit on the number of nodes in a cluster.',
      'A constraint on the total resource consumption (CPU, memory, objects) within a namespace.',
      'A cost management tool for cloud billing.',
      'A policy for auto-scaling resources in a namespace.',
    ],
    correctAnswerIndex: 1,
    explanation: 'ResourceQuotas limit total resource consumption and object counts within a namespace. "A limit on the number of nodes in a cluster" — node count is managed at the infrastructure level (e.g., Cluster Autoscaler), not ResourceQuota. "A cost management tool for cloud billing" — ResourceQuota is a Kubernetes API concept; cloud billing uses provider-specific tools. "A policy for auto-scaling resources in a namespace" — autoscaling uses HPA, VPA, or Cluster Autoscaler.',
  ),
  Question(
    questionText: 'What does kubectl rollout undo do?',
    options: [
      'Scales down a Deployment to zero replicas.',
      'Rolls back a Deployment to the previous revision.',
      'Removes a Deployment from the cluster.',
      'Restarts all pods in a Deployment.',
    ],
    correctAnswerIndex: 1,
    explanation: 'kubectl rollout undo reverts a Deployment to its previous revision, enabling quick rollbacks. "Scales down a Deployment to zero replicas" is done by kubectl scale deployment <name> --replicas=0. "Removes a Deployment from the cluster" is done by kubectl delete deployment <name>. "Restarts all pods in a Deployment" is done by kubectl rollout restart deployment <name>.',
  ),
  Question(
    questionText: 'Which Kubernetes API group handles authorization policies like Roles and ClusterRoles?',
    options: ['core', 'rbac.authorization.k8s.io', 'apps', 'networking.k8s.io'],
    correctAnswerIndex: 1,
    explanation: 'RBAC resources (Role, ClusterRole, RoleBinding, ClusterRoleBinding) belong to the rbac.authorization.k8s.io API group. "core" (empty group) contains fundamental resources like Pods, Services, ConfigMaps, and Secrets. "apps" contains workload resources like Deployments, StatefulSets, DaemonSets, and ReplicaSets. "networking.k8s.io" contains networking resources like Ingress and NetworkPolicy.',
  ),
  Question(
    questionText: 'What is the purpose of a StorageClass in Kubernetes?',
    options: [
      'To define access modes for pods.',
      'To describe the type of storage and allow dynamic provisioning of PersistentVolumes.',
      'To set encryption policies for volumes.',
      'To define backup schedules for persistent data.',
    ],
    correctAnswerIndex: 1,
    explanation: 'StorageClasses allow administrators to describe storage types, enabling dynamic PV provisioning on demand. "To define access modes for pods" — access modes (ReadWriteOnce, ReadWriteMany, etc.) are defined on the PVC and PV, not on StorageClass. "To set encryption policies for volumes" — encryption can be a StorageClass parameter on some platforms, but it is not the primary purpose. "To define backup schedules for persistent data" — backup scheduling uses external tools like Velero, not StorageClass.',
  ),
  Question(
    questionText: 'What is the difference between NodePort and LoadBalancer Service types?',
    options: [
      'NodePort creates a cloud load balancer; LoadBalancer exposes a port on each node.',
      'NodePort exposes a port on each node\'s IP; LoadBalancer provisions an external cloud load balancer.',
      'They are identical in function.',
      'NodePort is for internal traffic only; LoadBalancer is cluster-internal.',
    ],
    correctAnswerIndex: 1,
    explanation: 'NodePort opens a port on all node IPs; LoadBalancer provisions an external load balancer via the cloud provider. The first option reverses the roles — NodePort exposes ports on nodes, LoadBalancer creates a cloud LB. They are functionally different — LoadBalancer is a superset of NodePort plus cloud integration. NodePort is externally accessible (via node IP:port); LoadBalancer provides a more user-friendly external IP.',
  ),
  Question(
    questionText: 'What is kubectl port-forward used for?',
    options: [
      'Exposing a Service publicly on the internet.',
      'Forwarding a local port to a port on a pod for local debugging.',
      'Configuring Ingress routing rules.',
      'Transferring files between pods.',
    ],
    correctAnswerIndex: 1,
    explanation: 'kubectl port-forward tunnels traffic from a local port to a pod port for local development and debugging. "Exposing a Service publicly on the internet" requires a LoadBalancer or Ingress — port-forward is only for local access. "Configuring Ingress routing rules" is done by creating/modifying Ingress resources. "Transferring files between pods" is done with kubectl cp.',
  ),
  Question(
    questionText: 'What is a Kubernetes mutating admission webhook?',
    options: [
      'A webhook that blocks invalid pod specs.',
      'A webhook that can modify objects (e.g., inject sidecars) during the admission phase.',
      'A webhook triggered when a pod is deleted.',
      'A webhook that modifies node configurations.',
    ],
    correctAnswerIndex: 1,
    explanation: 'Mutating admission webhooks intercept API requests and can modify the object before it is persisted. "A webhook that blocks invalid pod specs" describes a validating admission webhook — it validates without modifying. "A webhook triggered when a pod is deleted" — admission webhooks act on create/update/delete requests at admission time, not after deletion. "A webhook that modifies node configurations" — node configurations are managed through the kubelet config or node objects, not admission webhooks.',
  ),
  Question(
    questionText: 'What is the role of kube-proxy on a node?',
    options: [
      'To authenticate API requests.',
      'To maintain network rules that implement Kubernetes Service VIPs using iptables or IPVS.',
      'To manage container images on the node.',
      'To report node health to the API server.',
    ],
    correctAnswerIndex: 1,
    explanation: 'kube-proxy maintains iptables/IPVS rules on each node to route Service traffic to the correct pod endpoints. "To authenticate API requests" — API authentication is handled by the kube-apiserver with tokens, certificates, or OIDC. "To manage container images on the node" is the role of the container runtime (e.g., containerd). "To report node health to the API server" is the role of the kubelet, which updates the node\'s status conditions.',
  ),
  Question(
    questionText: 'Which command shows real-time CPU and memory usage for pods?',
    options: ['kubectl describe pods', 'kubectl top pods', 'kubectl get pods -o wide', 'kubectl logs'],
    correctAnswerIndex: 1,
    explanation: 'kubectl top pods shows current CPU and memory consumption for pods (requires metrics-server). "kubectl describe pods" shows detailed pod configuration and events — not live resource metrics. "kubectl get pods -o wide" shows pod status and node assignment — not CPU/memory metrics. "kubectl logs" fetches container stdout/stderr logs — not resource usage data.',
  ),
  Question(
    questionText: 'What does the imagePullPolicy: Always setting do for a container?',
    options: [
      'Uses the cached local image and never pulls from the registry.',
      'Always pulls the image from the registry every time the pod starts, ignoring local cache.',
      'Pulls only if the tag is "latest".',
      'Applies the pull policy only to init containers.',
    ],
    correctAnswerIndex: 1,
    explanation: 'imagePullPolicy: Always forces a registry pull on every pod start, ensuring the latest image is always used. "Uses the cached local image and never pulls from the registry" describes imagePullPolicy: Never. "Pulls only if the tag is latest" describes the default behavior without explicit imagePullPolicy when using the latest tag. "Applies the pull policy only to init containers" — imagePullPolicy applies to any container spec where it is set.',
  ),
  Question(
    questionText: 'What is the purpose of a LimitRange in Kubernetes?',
    options: [
      'To set cluster-wide maximum node count.',
      'To enforce minimum and maximum resource constraints for containers, pods, or PVCs within a namespace.',
      'To limit the number of namespaces in a cluster.',
      'To set rate limits for API server requests.',
    ],
    correctAnswerIndex: 1,
    explanation: 'LimitRange sets default, min, and max CPU/memory values for containers or PVCs within a namespace. "To set cluster-wide maximum node count" — node count is managed by Cluster Autoscaler or cloud infrastructure settings. "To limit the number of namespaces in a cluster" — there is no built-in Kubernetes resource for this; it requires custom RBAC or admission policies. "To set rate limits for API server requests" — API rate limiting is configured in the kube-apiserver flags, not LimitRange.',
  ),
  Question(
    questionText: 'What is a Kubernetes Gateway API?',
    options: [
      'A legacy equivalent to Ingress using annotations.',
      'A more expressive, role-oriented API for managing HTTP and network traffic into the cluster.',
      'An API for managing cloud provider load balancers.',
      'A policy for blocking external gateway access.',
    ],
    correctAnswerIndex: 1,
    explanation: 'The Gateway API is a next-generation Kubernetes API for traffic routing that improves upon Ingress with richer features. "A legacy equivalent to Ingress using annotations" — the Gateway API is the modern replacement for Ingress, not a legacy version. "An API for managing cloud provider load balancers" — while it can configure cloud LBs through implementations, it is a Kubernetes API abstraction. "A policy for blocking external gateway access" — Gateway API defines routing rules, not denial policies; NetworkPolicy handles traffic blocking.',
  ),
  Question(
    questionText: 'What happens to a Kubernetes Pod when it exceeds its configured resource limits?',
    options: [
      'It is OOMKilled if it exceeds its memory limit, and throttled if it exceeds its CPU limit.',
      'It is throttled for memory and OOMKilled for CPU.',
      'It is evicted from the node regardless of whether it exceeds CPU or memory.',
      'It is restarted automatically with doubled limits.',
    ],
    correctAnswerIndex: 0,
    explanation: 'Kubernetes enforces memory limits strictly: if a container exceeds its memory limit, the kernel terminates it (OOMKilled). For CPU, limits are enforced by CFS shares/throttling, meaning the container is throttled rather than terminated. The second option reverses the behaviors — memory causes OOMKill, CPU causes throttling. Eviction happens when node resources are under pressure for all pods, not just when a single pod exceeds its limits. Kubernetes does not automatically increase limits — they are static unless VPA is configured.',
  ),
  Question(
    questionText: 'What is the primary role of a Kubernetes Operator in relation to Custom Resources?',
    options: [
      'To translate Custom Resources into standard Kubernetes resources and manage their lifecycle.',
      'To compile Custom Resources into container images.',
      'To schedule pods to nodes based on Custom Resource settings.',
      'To encrypt Custom Resource definitions in etcd.',
    ],
    correctAnswerIndex: 0,
    explanation: 'A Kubernetes Operator uses custom controllers to watch Custom Resources (CRs) and translate/reconcile them into standard Kubernetes objects (like Pods, Services, and Deployments) to reach the desired state. "To compile Custom Resources into container images" — Operators are software controllers, not compilers; CRs are API objects. "To schedule pods to nodes based on Custom Resource settings" — scheduling is done by the kube-scheduler; Operators may create pods but do not schedule them. "To encrypt Custom Resource definitions in etcd" — etcd encryption is a cluster-level security configuration, not an Operator responsibility.',
  ),
];
