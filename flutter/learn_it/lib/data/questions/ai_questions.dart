import '../../models/question.dart';

final List<Question> aiQuestions = [
  Question(
    questionText: 'What is the primary purpose of Supervised Learning?',
    options: ['Train on labeled input-output pairs', 'Find hidden structure in unlabeled data', 'Learn through rewards and penalties', 'Replace all math with rules'],
    correctAnswerIndex: 0,
    explanation: 'Supervised learning uses labeled examples to learn a mapping from inputs to outputs. "Find hidden structure in unlabeled data" describes Unsupervised Learning. "Learn through rewards and penalties" describes Reinforcement Learning. "Replace all math with rules" describes rule-based expert systems, not machine learning.',
  ),
  Question(
    questionText: 'Which AI paradigm discovers patterns without using labeled data?',
    options: ['Supervised Learning', 'Unsupervised Learning', 'Reinforcement Learning', 'Transfer Learning'],
    correctAnswerIndex: 1,
    explanation: 'Unsupervised learning finds hidden structure in data without labels. "Supervised Learning" requires labeled input-output pairs. "Reinforcement Learning" learns through rewards and environment interaction, not raw pattern discovery. "Transfer Learning" reuses a pre-trained model on a new task — it is not about label-free pattern discovery.',
  ),
  Question(
    questionText: 'In Reinforcement Learning, what does an agent maximize?',
    options: ['Loss function', 'Cumulative reward', 'Data volume', 'Number of parameters'],
    correctAnswerIndex: 1,
    explanation: 'An RL agent takes actions to maximize a cumulative reward signal over time. "Loss function" is minimized (not maximized) in supervised learning, not an RL objective. "Data volume" is not an agent goal — more data is a resource, not a reward. "Number of parameters" relates to model complexity and is not what an RL agent optimizes for.',
  ),
  Question(
    questionText: 'What is the role of backpropagation in neural networks?',
    options: ['Generate training data', 'Compute gradients and update weights', 'Normalize input data', 'Add new layers dynamically'],
    correctAnswerIndex: 1,
    explanation: 'Backpropagation propagates error signals backward through the network to update weights using gradients. "Generate training data" is data augmentation or synthesis, unrelated to backprop. "Normalize input data" is preprocessing (e.g., z-score normalization), done before training. "Add new layers dynamically" describes architecture search or progressive growing, not backpropagation.',
  ),
  Question(
    questionText: 'What does overfitting mean in a machine learning model?',
    options: ['Model generalizes well to new data', 'Model performs well on training data but poorly on unseen data', 'Model has too few parameters', 'Model converges too slowly'],
    correctAnswerIndex: 1,
    explanation: 'Overfitting occurs when a model memorizes training examples instead of learning general patterns. "Model generalizes well to new data" describes the desired behavior, not overfitting. "Model has too few parameters" describes underfitting, the opposite problem. "Model converges too slowly" is a training efficiency issue unrelated to overfitting.',
  ),
  Question(
    questionText: 'Which activation function outputs values between 0 and 1 and is common in binary classification output layers?',
    options: ['ReLU', 'Tanh', 'Sigmoid', 'Softmax'],
    correctAnswerIndex: 2,
    explanation: 'Sigmoid squashes output to (0,1), making it useful for binary classification probabilities. "ReLU" outputs values from 0 to infinity — it does not bound outputs to (0,1). "Tanh" outputs values between -1 and 1, not 0 and 1. "Softmax" outputs a probability distribution across multiple classes, not a single binary probability.',
  ),
  Question(
    questionText: 'What is Self-Attention in Transformer models?',
    options: ['A technique to prune network weights', 'A mechanism that lets tokens relate to each other within a sequence', 'A regularization method', 'A pooling operation over image patches'],
    correctAnswerIndex: 1,
    explanation: 'Self-attention computes relationships between all tokens in a sequence, enabling context-aware representations. "Pruning" removes unimportant weights from a trained model — unrelated to attention. "A regularization method" describes techniques like dropout or weight decay that prevent overfitting. "A pooling operation over image patches" describes Vision Transformer patch embedding, not the attention mechanism itself.',
  ),
  Question(
    questionText: 'What is tokenization in the context of NLP?',
    options: ['Encrypting input text', 'Splitting raw text into smaller units a model can process', 'Compressing model weights', 'Encoding labels into one-hot vectors'],
    correctAnswerIndex: 1,
    explanation: 'Tokenization converts raw text into tokens (words, subwords, or characters) for model input. "Encrypting input text" is a security operation, not a preprocessing step for NLP. "Compressing model weights" is model quantization or pruning, done after training. "Encoding labels into one-hot vectors" is label encoding for classification tasks, not input tokenization.',
  ),
  Question(
    questionText: 'Which metric measures how many predicted positives were actually correct?',
    options: ['Recall', 'F1 Score', 'Precision', 'Accuracy'],
    correctAnswerIndex: 2,
    explanation: 'Precision = TP / (TP + FP), measuring the correctness of positive predictions. "Recall" measures how many actual positives were correctly identified (TP / (TP + FN)). "F1 Score" is the harmonic mean of precision and recall, not just one of them. "Accuracy" measures correct predictions overall across all classes, not just the ratio of correct positives.',
  ),
  Question(
    questionText: 'What problem does Dropout address during neural network training?',
    options: ['Underfitting', 'Slow convergence', 'Overfitting', 'Vanishing gradients'],
    correctAnswerIndex: 2,
    explanation: 'Dropout randomly deactivates neurons during training to prevent co-adaptation and reduce overfitting. "Underfitting" is when a model is too simple — dropout would make underfitting worse, not better. "Slow convergence" is addressed by adaptive optimizers (e.g., Adam) or learning rate schedules. "Vanishing gradients" are addressed by activation functions like ReLU, skip connections, or batch normalization.',
  ),
  Question(
    questionText: 'What is transfer learning?',
    options: ['Moving data between databases', 'Reusing a pre-trained model on a new related task', 'Sharing weights between two unrelated tasks', 'Training a model on encrypted data'],
    correctAnswerIndex: 1,
    explanation: 'Transfer learning applies knowledge gained from one task to accelerate learning on another. "Moving data between databases" is a data engineering operation unrelated to model training. "Sharing weights between two unrelated tasks" is not transfer learning — tasks should be related for knowledge to transfer meaningfully. "Training a model on encrypted data" describes federated or privacy-preserving learning techniques.',
  ),
  Question(
    questionText: 'What is a confusion matrix used for?',
    options: ['Visualizing model architecture', 'Evaluating classification model performance across classes', 'Plotting loss curves', 'Measuring training speed'],
    correctAnswerIndex: 1,
    explanation: 'A confusion matrix shows counts of true positives, false positives, true negatives, and false negatives. "Visualizing model architecture" is done with network diagrams or tools like Netron. "Plotting loss curves" shows training/validation loss over epochs, not classification breakdown. "Measuring training speed" would involve timing benchmarks, not classification results.',
  ),
  Question(
    questionText: 'What is the vanishing gradient problem?',
    options: ['Gradients explode and destabilize training', 'Gradients become very small in early layers, slowing learning', 'The model forgets training data', 'Weights become negative'],
    correctAnswerIndex: 1,
    explanation: 'Vanishing gradients make early-layer weights barely update, hindering deep network training. "Gradients explode and destabilize training" is the exploding gradient problem — the opposite issue. "The model forgets training data" describes catastrophic forgetting in continual learning, not gradient flow. "Weights become negative" is expected during training and is not a problem on its own.',
  ),
  Question(
    questionText: 'What does the term "epoch" mean during model training?',
    options: ['One weight update step', 'One full pass through the entire training dataset', 'One layer forward pass', 'One hyperparameter tuning round'],
    correctAnswerIndex: 1,
    explanation: 'An epoch is a complete cycle through all training samples. "One weight update step" describes a single iteration or batch update (mini-batch gradient descent). "One layer forward pass" is just part of a single forward pass through the network, not an epoch. "One hyperparameter tuning round" describes one trial in a hyperparameter search, not a training epoch.',
  ),
  Question(
    questionText: 'Which technique addresses imbalanced classification datasets?',
    options: ['Batch normalization', 'Oversampling the minority class or undersampling the majority class', 'Increasing learning rate', 'Using ReLU activations'],
    correctAnswerIndex: 1,
    explanation: 'Techniques like SMOTE (oversampling) or random undersampling help balance class distributions. "Batch normalization" normalizes activations within a mini-batch to stabilize training — it does not fix class imbalance. "Increasing learning rate" affects optimization speed, not dataset balance. "Using ReLU activations" is an architecture choice unrelated to dataset imbalance.',
  ),
  Question(
    questionText: 'What is the purpose of a learning rate in gradient descent?',
    options: ['It sets the number of training epochs', 'It controls the step size for weight updates', 'It determines the number of hidden layers', 'It regularizes the loss function'],
    correctAnswerIndex: 1,
    explanation: 'The learning rate scales how much weights are adjusted in response to gradients. "It sets the number of training epochs" is a separate hyperparameter controlling how many full passes through the data. "It determines the number of hidden layers" is an architectural choice, not related to the optimizer. "It regularizes the loss function" describes regularization terms like L1/L2 penalties, not the learning rate.',
  ),
  Question(
    questionText: 'What does a Convolutional Neural Network (CNN) excel at?',
    options: ['Sequential time-series modeling', 'Translating text between languages', 'Extracting spatial features from images', 'Reinforcement learning policy optimization'],
    correctAnswerIndex: 2,
    explanation: 'CNNs use filters to detect local spatial patterns in image data. "Sequential time-series modeling" is the strength of RNNs and LSTMs, not CNNs. "Translating text between languages" is the strength of sequence-to-sequence Transformer models. "Reinforcement learning policy optimization" is handled by policy gradient methods, not CNNs specifically.',
  ),
  Question(
    questionText: 'Which evaluation metric balances precision and recall?',
    options: ['Accuracy', 'AUC-ROC', 'F1 Score', 'MSE'],
    correctAnswerIndex: 2,
    explanation: 'F1 score is the harmonic mean of precision and recall. "Accuracy" measures overall correct predictions but ignores the precision-recall trade-off, especially on imbalanced datasets. "AUC-ROC" measures classifier discrimination ability across all thresholds, not the balance of precision and recall at a specific threshold. "MSE" (Mean Squared Error) is a regression metric, not a classification metric.',
  ),
  Question(
    questionText: 'What is a Recurrent Neural Network (RNN) designed for?',
    options: ['Processing grid-structured data', 'Sequential or time-series data with temporal dependencies', 'Image segmentation', 'Clustering unlabeled data'],
    correctAnswerIndex: 1,
    explanation: 'RNNs maintain hidden state loops, making them suited for sequential data. "Processing grid-structured data" describes CNNs, which use convolutions on spatially organized inputs like images. "Image segmentation" uses specialized architectures like U-Net or FCN, not RNNs. "Clustering unlabeled data" is an unsupervised task handled by algorithms like K-Means, not RNNs.',
  ),
  Question(
    questionText: 'What is an embedding in machine learning?',
    options: ['A compression algorithm for images', 'A dense vector representation of discrete items in a continuous space', 'A type of activation function', 'A loss function for regression'],
    correctAnswerIndex: 1,
    explanation: 'Embeddings map high-dimensional discrete inputs (like words) to low-dimensional dense vectors. "A compression algorithm for images" describes codecs like JPEG or PNG — not embeddings. "A type of activation function" describes functions like ReLU or sigmoid that introduce non-linearity. "A loss function for regression" describes MSE or MAE used to measure prediction errors.',
  ),
  Question(
    questionText: 'What is the purpose of batch normalization?',
    options: ['Reducing dataset size', 'Normalizing layer activations to stabilize and speed up training', 'Preventing gradient explosion by clipping', 'Increasing model depth'],
    correctAnswerIndex: 1,
    explanation: 'Batch normalization normalizes activations within a mini-batch, reducing internal covariate shift. "Reducing dataset size" is data compression or pruning, not normalization. "Preventing gradient explosion by clipping" is gradient clipping, a separate technique applied directly to gradient values. "Increasing model depth" is an architectural decision unrelated to normalization.',
  ),
  Question(
    questionText: 'What is the key idea behind a Generative Adversarial Network (GAN)?',
    options: ['A model that learns by trial and error', 'A generator and discriminator trained adversarially to produce realistic data', 'A model that compresses data losslessly', 'A supervised model using labeled images'],
    correctAnswerIndex: 1,
    explanation: 'GANs pit a generator against a discriminator, driving the generator to produce increasingly realistic outputs. "A model that learns by trial and error" describes reinforcement learning with reward feedback. "A model that compresses data losslessly" describes lossless compression algorithms — GANs lose information during generation. "A supervised model using labeled images" describes image classification networks, not generative models.',
  ),
  Question(
    questionText: 'What does cross-entropy loss measure?',
    options: ['Mean squared distance between predictions and labels', 'Difference between predicted probability distribution and true distribution', 'Number of misclassified samples', 'Absolute difference between predicted and true values'],
    correctAnswerIndex: 1,
    explanation: 'Cross-entropy measures how well predicted probability distributions match true class distributions. "Mean squared distance between predictions and labels" is MSE, used for regression tasks. "Number of misclassified samples" describes raw error count, not a differentiable loss function. "Absolute difference between predicted and true values" is Mean Absolute Error (MAE), a regression loss.',
  ),
  Question(
    questionText: 'Which regularization method adds the sum of absolute values of weights to the loss?',
    options: ['L2 (Ridge)', 'L1 (Lasso)', 'Dropout', 'Early stopping'],
    correctAnswerIndex: 1,
    explanation: 'L1 regularization adds |w| to the loss, encouraging sparse weight vectors. "L2 (Ridge)" adds the sum of squared weights, penalizing large weights but not driving them to zero. "Dropout" randomly deactivates neurons during training — it does not add a weight penalty term to the loss. "Early stopping" halts training when validation performance stops improving — it is not a loss term.',
  ),
  Question(
    questionText: 'What is the purpose of a validation set during model training?',
    options: ['To compute final test accuracy', 'To tune hyperparameters and monitor for overfitting without touching the test set', 'To augment training data', 'To initialize model weights'],
    correctAnswerIndex: 1,
    explanation: 'A validation set provides unbiased evaluation during training to guide hyperparameter choices. "To compute final test accuracy" is the role of the test set, not the validation set — the test set should only be used once. "To augment training data" is data augmentation applied to the training set, not the validation set. "To initialize model weights" describes weight initialization strategies like Xavier or He initialization.',
  ),
  Question(
    questionText: 'Which clustering algorithm assigns each sample to the nearest centroid?',
    options: ['DBSCAN', 'K-Means', 'Hierarchical Clustering', 'Gaussian Mixture Model'],
    correctAnswerIndex: 1,
    explanation: 'K-Means iteratively assigns points to the nearest of K centroids and updates centroids. "DBSCAN" clusters points based on density and can find arbitrarily shaped clusters — it does not use centroids. "Hierarchical Clustering" builds a tree of clusters (dendrogram) by merging or splitting, not by centroid assignment. "Gaussian Mixture Model" assigns soft probabilistic memberships using Gaussian distributions, not hard centroid assignment.',
  ),
  Question(
    questionText: 'What is principal component analysis (PCA) used for?',
    options: ['Training deep networks faster', 'Reducing dimensionality while preserving maximum variance', 'Generating synthetic samples', 'Optimizing hyperparameters'],
    correctAnswerIndex: 1,
    explanation: 'PCA projects data onto orthogonal components that capture the most variance. "Training deep networks faster" is addressed by optimizers or hardware, not PCA. "Generating synthetic samples" is data augmentation or generative models like GANs. "Optimizing hyperparameters" describes techniques like grid search or Bayesian optimization, not PCA.',
  ),
  Question(
    questionText: 'In a decision tree, what does information gain measure?',
    options: ['The depth of the tree', 'The reduction in entropy achieved by a split', 'The number of leaf nodes', 'The accuracy on the test set'],
    correctAnswerIndex: 1,
    explanation: 'Information gain measures how much a split reduces the entropy of the target variable. "The depth of the tree" is a structural property of the tree, not a split quality metric. "The number of leaf nodes" is another structural property — more leaves means a deeper, more complex tree. "The accuracy on the test set" is a generalization metric evaluated after training, not during tree construction.',
  ),
  Question(
    questionText: 'What is the softmax function used for?',
    options: ['Clipping gradient values', 'Converting raw scores to a probability distribution across classes', 'Normalizing input features', 'Pooling feature maps'],
    correctAnswerIndex: 1,
    explanation: 'Softmax exponentiates and normalizes logits so they sum to 1, giving class probabilities. "Clipping gradient values" is gradient clipping, used to prevent exploding gradients during training. "Normalizing input features" is feature scaling (e.g., z-score normalization), applied to input data before training. "Pooling feature maps" is a CNN operation (e.g., max pooling) that reduces spatial dimensions.',
  ),
  Question(
    questionText: 'What is fine-tuning in the context of large language models?',
    options: ['Training a model from scratch on a specific task', 'Adjusting a pre-trained model on a smaller domain-specific dataset', 'Removing layers from a neural network', 'Quantizing model weights to reduce size'],
    correctAnswerIndex: 1,
    explanation: 'Fine-tuning adapts a pre-trained LLM to a specific downstream task with additional training. "Training a model from scratch on a specific task" is pre-training, not fine-tuning — it ignores existing knowledge. "Removing layers from a neural network" is pruning, used to reduce model complexity. "Quantizing model weights to reduce size" is quantization, a compression technique applied after training.',
  ),
  Question(
    questionText: 'What distinguishes a Support Vector Machine (SVM) from logistic regression?',
    options: ['SVM cannot handle non-linear data', 'SVM finds a maximum-margin hyperplane, logistic regression models class probabilities', 'Logistic regression does not produce a decision boundary', 'SVM is only for regression tasks'],
    correctAnswerIndex: 1,
    explanation: 'SVM optimizes the margin between classes, while logistic regression maximizes likelihood of labels. "SVM cannot handle non-linear data" is false — the kernel trick allows SVMs to work in non-linear spaces. "Logistic regression does not produce a decision boundary" is false — logistic regression does produce a linear decision boundary. "SVM is only for regression tasks" is false — SVM is primarily a classification algorithm (SVR is its regression variant).',
  ),
  Question(
    questionText: 'What is the purpose of an attention mask in transformer models?',
    options: ['To speed up inference on GPUs', 'To prevent the model from attending to padding tokens', 'To randomly zero out attention weights during training', 'To select the most important layer'],
    correctAnswerIndex: 1,
    explanation: 'Attention masks tell the model to ignore padding positions in batched sequences. "To speed up inference on GPUs" is achieved by hardware optimization (e.g., FlashAttention), not the attention mask. "To randomly zero out attention weights during training" describes attention dropout, a different regularization technique. "To select the most important layer" is not a standard operation — attention masks operate on token positions, not layers.',
  ),
  Question(
    questionText: 'Which technique splits training data into K subsets to evaluate model generalizability?',
    options: ['Bootstrap sampling', 'K-Fold Cross-Validation', 'Hold-out split', 'Monte Carlo sampling'],
    correctAnswerIndex: 1,
    explanation: 'K-Fold CV rotates which fold is used as validation, giving a more robust performance estimate. "Bootstrap sampling" resamples with replacement to estimate statistics, but does not systematically rotate validation folds. "Hold-out split" divides data into train/test once — it is less robust than K-Fold because the result depends on the single split. "Monte Carlo sampling" randomly samples subsets repeatedly but does not guarantee each sample appears in validation exactly once.',
  ),
  Question(
    questionText: 'What is an autoencoder used for?',
    options: ['Supervised classification of images', 'Learning compressed representations and reconstructing data', 'Optimizing reward in environments', 'Generating text via autoregressive decoding'],
    correctAnswerIndex: 1,
    explanation: 'Autoencoders learn to compress inputs into a latent space and reconstruct them, useful for denoising and feature learning. "Supervised classification of images" uses CNNs trained with labeled data — autoencoders are typically unsupervised. "Optimizing reward in environments" describes reinforcement learning agents. "Generating text via autoregressive decoding" describes language models like GPT, not autoencoders.',
  ),
  Question(
    questionText: 'What is the role of the optimizer in neural network training?',
    options: ['Prepares the training data batches', 'Updates model weights based on computed gradients', 'Evaluates model on the test set', 'Defines the network architecture'],
    correctAnswerIndex: 1,
    explanation: 'Optimizers (e.g., Adam, SGD) use gradients to iteratively update model parameters to minimize loss. "Prepares the training data batches" is done by data loaders, not the optimizer. "Evaluates model on the test set" is the role of the evaluation loop, not the optimizer. "Defines the network architecture" is done when building the model — the optimizer only adjusts weights after the architecture is defined.',
  ),
  Question(
    questionText: 'What is RLHF (Reinforcement Learning from Human Feedback)?',
    options: ['Training a robot using simulated physics', 'Aligning LLM outputs with human preferences using RL on human ratings', 'Using cameras to train RL agents', 'Fine-tuning on labeled medical records'],
    correctAnswerIndex: 1,
    explanation: 'RLHF uses human preference data to train a reward model, then RL optimizes the LLM against it. "Training a robot using simulated physics" is sim-to-real transfer in robotics RL — not RLHF. "Using cameras to train RL agents" describes visual RL with pixel observations — unrelated to language model alignment. "Fine-tuning on labeled medical records" is domain-specific supervised fine-tuning, not RL from human feedback.',
  ),
  Question(
    questionText: 'What does "inference" mean in machine learning?',
    options: ['The process of training a new model', 'Using a trained model to make predictions on new data', 'Evaluating a model on the training set', 'Selecting the best architecture'],
    correctAnswerIndex: 1,
    explanation: 'Inference is when a trained model is applied to unseen input to produce predictions or outputs. "The process of training a new model" is the training phase, where weights are learned — not inference. "Evaluating a model on the training set" is training-set evaluation, not inference on new data. "Selecting the best architecture" is neural architecture search, a design-time activity.',
  ),
  Question(
    questionText: 'What is semantic segmentation in computer vision?',
    options: ['Detecting objects with bounding boxes', 'Classifying each pixel of an image into a category', 'Generating captions for images', 'Estimating image depth'],
    correctAnswerIndex: 1,
    explanation: 'Semantic segmentation assigns a class label to every pixel in an image. "Detecting objects with bounding boxes" is object detection (e.g., YOLO, Faster R-CNN), which localizes but does not label every pixel. "Generating captions for images" is image captioning, a cross-modal generation task. "Estimating image depth" is monocular depth estimation, which predicts distance per pixel, not class labels.',
  ),
  Question(
    questionText: 'Which ensemble method trains models sequentially, each correcting errors of the previous one?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Random Forest'],
    correctAnswerIndex: 1,
    explanation: 'Boosting builds models in sequence, with each model focusing on the errors of its predecessor. "Bagging" trains models in parallel on bootstrapped subsets and averages predictions — there is no sequential error correction. "Stacking" trains a meta-model on base model outputs, but models are not trained to correct each other\'s errors. "Random Forest" is a bagging method using decision trees — models are trained independently, not sequentially.',
  ),
  Question(
    questionText: 'What is a hyperparameter in machine learning?',
    options: ['A weight learned during training', 'A configuration set before training that controls the learning process', 'An output neuron in the final layer', 'A feature selected during preprocessing'],
    correctAnswerIndex: 1,
    explanation: 'Hyperparameters (e.g., learning rate, number of layers) are set before training and govern how the model learns. "A weight learned during training" is a model parameter, updated by backpropagation — not a hyperparameter. "An output neuron in the final layer" is a model architecture component, not a hyperparameter. "A feature selected during preprocessing" is a result of feature selection, not a training configuration.',
  ),
  Question(
    questionText: 'What is the purpose of LSTM (Long Short-Term Memory) networks?',
    options: ['Processing grid data with convolutions', 'Mitigating vanishing gradients in sequential data using gated memory cells', 'Distributing training across multiple GPUs', 'Generating images from text prompts'],
    correctAnswerIndex: 1,
    explanation: 'LSTMs use input, forget, and output gates to control information flow, enabling learning of long-range dependencies. "Processing grid data with convolutions" describes CNNs, designed for spatial data like images. "Distributing training across multiple GPUs" is data parallelism or model parallelism, a training infrastructure technique. "Generating images from text prompts" describes diffusion models or GANs conditioned on text — not LSTMs.',
  ),
  Question(
    questionText: 'Which type of learning creates its own labels from data structure without human annotation?',
    options: ['Supervised Learning', 'Self-supervised Learning', 'Reinforcement Learning', 'Semi-supervised Learning'],
    correctAnswerIndex: 1,
    explanation: 'Self-supervised learning creates labels from the data itself (e.g., predicting masked words). "Supervised Learning" requires human-annotated labels — it does not create labels automatically. "Reinforcement Learning" uses environment-provided reward signals, not internally created labels from data. "Semi-supervised Learning" uses a small amount of labeled data combined with large unlabeled data — it still relies on some human labels.',
  ),
  Question(
    questionText: 'What is model quantization?',
    options: ['Adding more layers to a model', 'Reducing numerical precision of weights to decrease model size and speed up inference', 'Measuring model accuracy with metrics', 'Augmenting training data with synthetic examples'],
    correctAnswerIndex: 1,
    explanation: 'Quantization converts weights from high precision (float32) to lower precision (int8) for efficient deployment. "Adding more layers to a model" increases model capacity and size — the opposite of quantization. "Measuring model accuracy with metrics" is model evaluation, not compression. "Augmenting training data with synthetic examples" is data augmentation, applied before or during training.',
  ),
  Question(
    questionText: 'Which algorithm is commonly used for hyperparameter search by modeling the objective as a probabilistic function?',
    options: ['Grid Search', 'Random Search', 'Bayesian Optimization', 'Genetic Algorithms'],
    correctAnswerIndex: 2,
    explanation: 'Bayesian optimization builds a probabilistic surrogate model to efficiently search the hyperparameter space. "Grid Search" exhaustively tries all combinations of predefined values — it does not model the objective probabilistically. "Random Search" samples hyperparameters randomly — it is more efficient than grid search but still not model-based. "Genetic Algorithms" use evolutionary selection and mutation — they do not build a probabilistic model of the objective.',
  ),
  Question(
    questionText: 'What is the purpose of data augmentation in training image models?',
    options: ['Reducing model size', 'Artificially expanding the training set with transformed versions of images', 'Normalizing pixel values to zero mean', 'Removing duplicate samples'],
    correctAnswerIndex: 1,
    explanation: 'Data augmentation applies transforms (flips, crops, rotations) to increase training diversity and reduce overfitting. "Reducing model size" is achieved by pruning or quantization, not augmentation. "Normalizing pixel values to zero mean" is preprocessing/normalization, not augmentation — it changes scale, not variety. "Removing duplicate samples" is data deduplication, which reduces dataset size rather than expanding it.',
  ),
  Question(
    questionText: 'What does AUC-ROC measure?',
    options: ['Average loss over training epochs', 'The ability of a classifier to distinguish between classes across all thresholds', 'Mean absolute error of regression predictions', 'Speed of model inference'],
    correctAnswerIndex: 1,
    explanation: 'AUC-ROC measures the area under the ROC curve, indicating overall classifier discrimination ability. "Average loss over training epochs" is a training curve metric, not a classification performance measure. "Mean absolute error of regression predictions" is MAE, a regression metric — not applicable to binary classification ranking. "Speed of model inference" is measured in latency or throughput benchmarks, not AUC.',
  ),
  Question(
    questionText: 'Which AI technique generates new text by predicting the next token given previous tokens?',
    options: ['Clustering', 'Autoregressive language modeling', 'Variational encoding', 'Contrastive learning'],
    correctAnswerIndex: 1,
    explanation: 'Autoregressive language models (like GPT) generate text by predicting one token at a time conditioned on prior tokens. "Clustering" is an unsupervised method for grouping similar data points — it does not generate text. "Variational encoding" describes variational autoencoders (VAEs), which learn latent distributions and reconstruct data — not sequential text generation. "Contrastive learning" trains representations by comparing similar and dissimilar examples — it is not a text generation method.',
  ),
  Question(
    questionText: 'What is the purpose of weight initialization in deep neural networks?',
    options: ['To assign random labels to training data', 'To set initial parameter values that promote stable gradient flow from the start', 'To configure the optimizer learning rate schedule', 'To define the loss function for training'],
    correctAnswerIndex: 1,
    explanation: 'Good initialization (e.g., He, Xavier) prevents vanishing/exploding gradients at the start of training. "To assign random labels to training data" describes label randomization used in some generalization experiments — not weight init. "To configure the optimizer learning rate schedule" is done separately through optimizer configuration, not weight initialization. "To define the loss function for training" is a model design decision (e.g., cross-entropy, MSE) — unrelated to weight initialization.',
  ),
  Question(
    questionText: 'What is the "bias-variance tradeoff" in machine learning?',
    options: ['The tradeoff between training speed and test accuracy', 'The balance between model simplicity (high bias) and sensitivity to data (high variance)', 'The relationship between GPU usage and model size', 'The balance between precision and recall'],
    correctAnswerIndex: 1,
    explanation: 'High bias means underfitting; high variance means overfitting. Good models balance both. "The tradeoff between training speed and test accuracy" describes computational efficiency trade-offs, not the bias-variance concept. "The relationship between GPU usage and model size" is a hardware resource concern, not a statistical learning concept. "The balance between precision and recall" describes the precision-recall trade-off in classification metrics.',
  ),
  Question(
    questionText: 'What does a Random Forest do to reduce variance compared to a single decision tree?',
    options: ['Uses deeper trees with no pruning', 'Trains many trees on bootstrapped data and averages their predictions', 'Trains trees sequentially each correcting the last', 'Uses a single tree but with more features'],
    correctAnswerIndex: 1,
    explanation: 'Random Forests average predictions from many diverse trees, reducing variance via ensemble aggregation. "Uses deeper trees with no pruning" increases variance in a single tree — the opposite of what Random Forest achieves. "Trains trees sequentially each correcting the last" describes Boosting (e.g., XGBoost), not Random Forest. "Uses a single tree but with more features" would still give a single high-variance tree, not an ensemble.',
  ),
  Question(
    questionText: 'What is the purpose of the temperature parameter in language model generation?',
    options: ['To control training speed', 'To scale logits before softmax, controlling randomness of outputs', 'To adjust batch size during inference', 'To set the number of attention heads'],
    correctAnswerIndex: 1,
    explanation: 'Higher temperature makes output more random/creative; lower temperature makes it more deterministic. "To control training speed" is done via learning rate or batch size — temperature is only used at inference time. "To adjust batch size during inference" is a hardware/memory setting, separate from generation randomness. "To set the number of attention heads" is a fixed architectural hyperparameter, not a dynamic inference parameter.',
  ),
  Question(
    questionText: 'What are the three primary types of layers in a Convolutional Neural Network (CNN)?',
    options: [
      'Convolutional, Pooling, and Fully Connected layers',
      'Input, Hidden, and Output layers',
      'Recurrent, Dense, and Dropout layers',
      'Activation, Normalization, and Attention layers',
    ],
    correctAnswerIndex: 0,
    explanation: 'A standard CNN architecture is composed of Convolutional layers (to extract features), Pooling layers (to reduce spatial dimensions), and Fully Connected (Dense) layers (to perform final classification). "Input, Hidden, and Output layers" is a generic description of any neural network, not specific to CNNs. "Recurrent, Dense, and Dropout layers" describes components of RNN architectures, not CNNs. "Activation, Normalization, and Attention layers" are secondary components found in many architectures — they are not the defining primary layer types of a CNN.',
  ),
];
