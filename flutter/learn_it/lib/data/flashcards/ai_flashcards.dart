import '../../models/flashcard.dart';

final List<Flashcard> aiFlashcards = [
  Flashcard(
    frontTitle: 'What is the Transformer self-attention mechanism?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Self-Attention',
    backExplanation: 'Self-attention allows each token in a sequence to weigh the importance of every other token when building its representation. It computes Query (Q), Key (K), and Value (V) matrices from the input and applies: Attention(Q,K,V) = softmax(QKᵀ/√d_k)V.',
    bulletPoints: [
      'Parallel computation — unlike RNNs, processes all tokens simultaneously.',
      '√d_k scaling prevents vanishing gradients in the softmax from large dot products.',
      'Multi-head attention runs H independent attention heads, concatenating their outputs.',
    ],
    codeSnippet: 'Attention(Q, K, V) = softmax( Q × Kᵀ / √d_k ) × V\n\nMulti-Head: Concat(head₁, ..., headₕ) × Wᴼ\nwhere headᵢ = Attention(QWᵢQ, KWᵢK, VWᵢV)',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is gradient descent?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Gradient Descent',
    backExplanation: 'An optimization algorithm that iteratively adjusts model weights in the direction of steepest descent of the loss function. At each step: θ = θ - α∇L(θ), where α is the learning rate and ∇L is the gradient of the loss.',
    bulletPoints: [
      'Batch GD: uses entire dataset per step — slow but stable.',
      'Stochastic GD (SGD): uses one sample per step — noisy but fast.',
      'Mini-batch GD: uses a small batch (32-512 samples) — best of both worlds, most common.',
    ],
    codeSnippet: 'θ = θ - α × ∇L(θ)\n\nwhere:\n  θ = model weights\n  α = learning rate\n  ∇L = gradient of loss w.r.t. θ',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is overfitting and how do you prevent it?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Overfitting',
    backExplanation: 'Overfitting occurs when a model learns the training data too specifically — including noise — and fails to generalize to new data. Symptoms: very low training loss but high validation loss.',
    bulletPoints: [
      'Regularization: L1 (Lasso, induces sparsity) or L2 (Ridge, penalizes large weights).',
      'Dropout: randomly zero out activations during training to prevent co-adaptation.',
      'Early Stopping: halt training when validation loss stops improving.',
      'Data Augmentation: synthetically expand training set with transforms.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is the difference between precision and recall?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Precision vs. Recall',
    backExplanation: 'Precision answers "of all items we predicted positive, how many were actually positive?" — minimizing false positives. Recall answers "of all actual positives, how many did we correctly identify?" — minimizing false negatives.',
    bulletPoints: [
      'Precision = TP / (TP + FP). High precision → few false alarms.',
      'Recall = TP / (TP + FN). High recall → few missed positives.',
      'F1 = 2 * (P * R) / (P + R) — harmonic mean, useful for imbalanced datasets.',
    ],
    codeSnippet: 'Precision = TP / (TP + FP)\nRecall    = TP / (TP + FN)\nF1        = 2*(Prec*Rec)/(Prec+Rec)\n\nExample: Cancer test\n  High Recall = catch all cases (OK to false alarm)\n  High Precision = only flag true cases (OK to miss some)',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Convolutional Neural Network (CNN)?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Convolutional Neural Network',
    backExplanation: 'A deep learning architecture specialized for processing grid-like data (images, time-series). Uses learnable convolutional filters to detect local features (edges, textures) hierarchically. Much more parameter-efficient than fully connected networks for images.',
    bulletPoints: [
      'Convolution layer: applies filters across spatial dimensions to detect features.',
      'Pooling (Max/Avg): downsamples feature maps to reduce spatial dimensions and computation.',
      'Feature hierarchy: early layers detect edges/colors; deeper layers detect shapes, objects.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is transfer learning?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Transfer Learning',
    backExplanation: 'Using a model pre-trained on a large dataset as the starting point for a related task, rather than training from scratch. The pre-trained model\'s learned representations (features) transfer to the new task, requiring far less data and compute.',
    bulletPoints: [
      'Feature extraction: freeze pre-trained layers, only train the new head.',
      'Fine-tuning: unfreeze some/all pre-trained layers and train with a small learning rate.',
      'Domain shift: if source and target domains are very different, fine-tuning may not help.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is RAG (Retrieval-Augmented Generation)?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Retrieval-Augmented Generation',
    backExplanation: 'RAG combines a retrieval system (vector search over a knowledge base) with a generative LLM. When a question is asked, relevant documents are retrieved and injected into the LLM prompt as context, grounding the response in real data.',
    bulletPoints: [
      'Solves hallucination: LLM answers from retrieved facts rather than parametric memory.',
      'Knowledge updates: no model retraining needed — just update the document store.',
      'Components: Embedder → Vector DB (retrieval) → LLM (generation).',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is the vanishing gradient problem?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Vanishing Gradient Problem',
    backExplanation: 'During backpropagation through deep networks, gradients shrink exponentially as they travel backward through layers — especially with Sigmoid or Tanh activations. Early layers receive near-zero gradients and stop learning.',
    bulletPoints: [
      'Solved by: ReLU activation (constant gradient for positive inputs).',
      'Residual connections (ResNet): shortcut paths that let gradients flow directly.',
      'Batch Normalization: stabilizes layer inputs, reducing gradient sensitivity.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a confusion matrix?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Confusion Matrix',
    backExplanation: 'A table that summarizes the performance of a classification model by showing counts of True Positives (TP), False Positives (FP), True Negatives (TN), and False Negatives (FN) across predicted vs. actual classes.',
    bulletPoints: [
      'Diagonal = correct predictions; off-diagonal = errors.',
      'Multi-class: N×N matrix where N = number of classes.',
      'Accuracy = (TP+TN) / Total. Not reliable for imbalanced datasets.',
    ],
    codeSnippet: 'Confusion Matrix (Binary):\n             Pred +    Pred -\nActual +  [   TP   |   FN   ]\nActual -  [   FP   |   TN   ]\n\nAccuracy  = (TP + TN) / (TP+FP+TN+FN)\nPrecision = TP / (TP + FP)\nRecall    = TP / (TP + FN)',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is LoRA fine-tuning?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'LoRA — Low-Rank Adaptation',
    backExplanation: 'A parameter-efficient fine-tuning method for LLMs. Instead of updating all model weights, LoRA injects small trainable low-rank matrices (A and B) into each transformer layer. The original weights are frozen — only A and B are trained.',
    bulletPoints: [
      'Weight update: ΔW = A × B, where A ∈ ℝ^(d×r), B ∈ ℝ^(r×k), rank r << d.',
      'Drastically reduces trainable parameters — from billions to millions.',
      'QLoRA: quantizes base model to 4-bit and applies LoRA — fine-tune 65B models on a single GPU.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is RLHF?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Reinforcement Learning from Human Feedback',
    backExplanation: 'A training methodology where a reward model is trained on human preference labels, and the LLM is then fine-tuned using RL (typically PPO) to maximize reward. Used to align LLMs to be helpful, harmless, and honest.',
    bulletPoints: [
      'Step 1: Supervised Fine-Tuning (SFT) on high-quality demonstrations.',
      'Step 2: Train a Reward Model on human preference rankings of outputs.',
      'Step 3: Optimize the SFT model via PPO to maximize reward model score.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is the bias-variance tradeoff?',
    frontSubtitle: 'AI CONCEPT',
    backTitle: 'Bias-Variance Tradeoff',
    backExplanation: 'A fundamental ML challenge where reducing bias (underfitting) tends to increase variance (overfitting) and vice versa. Total error = Bias² + Variance + Irreducible Noise.',
    bulletPoints: [
      'High Bias: model is too simple, misses the data pattern. (e.g., linear model on non-linear data).',
      'High Variance: model is too complex, overfits training noise. (e.g., deep tree on small dataset).',
      'Goal: find the sweet spot — complex enough to fit data, simple enough to generalize.',
    ],
    isConcept: true,
  ),
];
