import '../../models/flashcard.dart';

final List<Flashcard> kubernetesFlashcards = [
  Flashcard(
    frontTitle: 'What is a Kubernetes Pod?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Pod',
    backExplanation: 'The smallest deployable unit in Kubernetes. A Pod encapsulates one or more containers that share the same network namespace (IP address, ports) and storage volumes. Containers in a Pod communicate via localhost.',
    bulletPoints: [
      'Ephemeral: Pods are not self-healing. If a Pod dies, it is replaced (not restarted) by a controller.',
      'Init Containers: Special containers that run to completion before app containers start.',
      'Sidecar pattern: helper container (e.g., log shipper) runs alongside the main container in one Pod.',
    ],
    codeSnippet: '# Quick Pod spec\napiVersion: v1\nkind: Pod\nspec:\n  containers:\n  - name: app\n    image: nginx:alpine\n    ports:\n    - containerPort: 80\n    resources:\n      requests:\n        cpu: "100m"\n        memory: "128Mi"\n      limits:\n        cpu: "500m"\n        memory: "256Mi"',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Deployment?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Deployment',
    backExplanation: 'A controller that declaratively manages a set of identical Pod replicas via a ReplicaSet. Deployments handle rolling updates, rollbacks, and scaling automatically.',
    bulletPoints: [
      'Rolling update strategy: gradually replaces old Pods with new ones (maxSurge/maxUnavailable configurable).',
      'Recreate strategy: terminates all old Pods before starting new ones (causes downtime).',
      'Rollback: kubectl rollout undo reverts to any previous revision.',
    ],
    codeSnippet: '# Scale a deployment and check rollout status\nkubectl scale deployment app --replicas=5\nkubectl rollout status deployment/app\nkubectl rollout undo deployment/app  # rollback',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What are Kubernetes resource Requests and Limits?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Resource Requests & Limits',
    backExplanation: 'Requests are what the container is guaranteed to get — used by the scheduler to find a suitable node. Limits are the maximum a container can consume before being throttled (CPU) or OOM-killed (memory).',
    bulletPoints: [
      'CPU is compressible: exceeding limit throttles. Memory is incompressible: exceeding limit = OOMKilled.',
      'LimitRange: set default requests/limits for a namespace.',
      'ResourceQuota: cap total resource consumption across all Pods in a namespace.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Service?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Kubernetes Service',
    backExplanation: 'An abstraction providing a stable network identity (DNS name + virtual IP) for a dynamic set of Pods, selected via label selectors. Enables load balancing across Pod replicas.',
    bulletPoints: [
      'ClusterIP (default): internal cluster access only via virtual IP.',
      'NodePort: exposes service on a static port on every node (30000-32767).',
      'LoadBalancer: provisions cloud load balancer with external IP.',
      'ExternalName: maps Service to an external DNS name (no proxying).',
    ],
    codeSnippet: 'apiVersion: v1\nkind: Service\nspec:\n  selector:\n    app: frontend\n  type: ClusterIP\n  ports:\n  - port: 80\n    targetPort: 8080',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes ConfigMap?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'ConfigMap',
    backExplanation: 'An API object storing non-sensitive configuration as key-value pairs. Decouples configuration from container images — change config without rebuilding images.',
    bulletPoints: [
      'Mounted as environment variables or as volume files inside Pods.',
      'Not encrypted — do not store passwords or secrets in ConfigMaps.',
      'ConfigMap updates: env vars do NOT auto-update; volume mounts do (with kubelet sync delay).',
    ],
    codeSnippet: '# Create configmap from literals\nkubectl create configmap app-config \\\n  --from-literal=ENV=production \\\n  --from-literal=LOG_LEVEL=info\n\n# Use in Pod env\nenv:\n- name: ENV\n  valueFrom:\n    configMapKeyRef:\n      name: app-config\n      key: ENV',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Secret?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Kubernetes Secret',
    backExplanation: 'Similar to ConfigMap but intended for sensitive data (passwords, tokens, certificates). Values are Base64-encoded — NOT encrypted by default. Enable Encryption at Rest and use external secret managers (Vault, AWS Secrets Manager) for production.',
    bulletPoints: [
      'Types: Opaque (generic), kubernetes.io/dockerconfigjson (registry creds), kubernetes.io/tls (TLS certs).',
      'External Secrets Operator: syncs secrets from Vault, AWS, GCP into Kubernetes Secrets automatically.',
      'Never commit Secret YAML to Git — use sealed secrets or Vault dynamic secrets.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Namespace?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Namespace',
    backExplanation: 'A virtual cluster inside a physical cluster providing resource isolation. Namespaces scope names (Pods, Services, Deployments) — the same name can exist in different namespaces. RBAC and ResourceQuotas are applied per namespace.',
    bulletPoints: [
      'Default namespaces: default, kube-system, kube-public, kube-node-lease.',
      'Cross-namespace Service DNS: my-service.other-namespace.svc.cluster.local.',
      'Cluster-scoped resources (Nodes, PersistentVolumes) are NOT namespaced.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Ingress?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Kubernetes Ingress',
    backExplanation: 'An API object managing external HTTP/HTTPS access to services within the cluster. Acts as a Layer 7 reverse proxy — routes by hostname or URL path. Requires an Ingress Controller (nginx, Traefik, HAProxy) to function.',
    bulletPoints: [
      'Path-based routing: /api → backend-svc; / → frontend-svc.',
      'Host-based routing: api.example.com → api-svc; app.example.com → app-svc.',
      'TLS termination: store certificate in a Secret, reference in Ingress TLS config.',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is the Kubernetes etcd?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'etcd',
    backExplanation: 'A distributed, consistent key-value store used as Kubernetes\'s backing store for all cluster state data — desired configurations, actual state, secrets, and metadata. If etcd is lost, the cluster state is lost.',
    bulletPoints: [
      'Uses the Raft consensus algorithm for strong consistency.',
      'Run with 3 or 5 members for quorum-based HA (tolerates 1 or 2 failures).',
      'Back up etcd regularly with etcdctl snapshot save in production clusters.',
    ],
    codeSnippet: '# Snapshot etcd for backup\nETCDCTL_API=3 etcdctl snapshot save snapshot.db \\\n  --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/etcd/ca.crt \\\n  --cert=/etc/etcd/server.crt \\\n  --key=/etc/etcd/server.key',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes DaemonSet?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'DaemonSet',
    backExplanation: 'Ensures that a copy of a specific Pod runs on every (or a selected subset of) node in the cluster. Automatically adds Pods to new nodes and removes them from deleted nodes.',
    bulletPoints: [
      'Use cases: log collectors (Fluentd), monitoring agents (Prometheus node-exporter), network plugins (CNI).',
      'Node selector or affinity/tolerations can limit which nodes the DaemonSet targets.',
      'Update strategy: RollingUpdate (default) or OnDelete (manual node-by-node).',
    ],
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is Horizontal Pod Autoscaler (HPA)?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Horizontal Pod Autoscaler',
    backExplanation: 'Automatically scales the number of Pod replicas in a Deployment or StatefulSet based on observed CPU utilization, memory, or custom metrics from the Metrics Server or Prometheus.',
    bulletPoints: [
      'Checks metrics every 15 seconds by default.',
      'Scaling formula: desiredReplicas = ceil(currentReplicas × currentMetric / targetMetric).',
      'KEDA (Kubernetes Event-Driven Autoscaler): scale-to-zero based on external queue depth, Pub/Sub, etc.',
    ],
    codeSnippet: '# Create HPA targeting 50% CPU\nkubectl autoscale deployment app \\\n  --cpu-percent=50 \\\n  --min=2 \\\n  --max=20\n\n# Check current HPA status\nkubectl get hpa',
    isConcept: true,
  ),
  Flashcard(
    frontTitle: 'What is a Kubernetes Taint and Toleration?',
    frontSubtitle: 'KUBERNETES CONCEPT',
    backTitle: 'Taints & Tolerations',
    backExplanation: 'Taints are applied to nodes to repel Pods that don\'t tolerate them. Tolerations are applied to Pods to allow scheduling on tainted nodes. Together they control which Pods can run on which nodes.',
    bulletPoints: [
      'Taint effect NoSchedule: new Pods without toleration won\'t be scheduled.',
      'Taint effect NoExecute: evicts existing Pods without toleration.',
      'Node Affinity: complements taints — attract Pods to specific nodes (vs repel).',
    ],
    codeSnippet: '# Taint a node for GPU workloads only\nkubectl taint nodes gpu-node-1 gpu=true:NoSchedule\n\n# Toleration in Pod spec\ntolerations:\n- key: "gpu"\n  operator: "Equal"\n  value: "true"\n  effect: "NoSchedule"',
    isConcept: true,
  ),
];
