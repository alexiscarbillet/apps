import '../../models/cheatsheet.dart';

final Cheatsheet awsCheatsheet = Cheatsheet(
  category: 'AWS',
  summary: 'Amazon Web Services (AWS) is a comprehensive cloud platform. This cheatsheet covers compute, storage, databases, networking, serverless, monitoring, and security fundamentals.',
  sections: [
    CheatsheetSection(
      title: 'EC2 & Compute Services',
      content: 'Elastic Compute Cloud (EC2) provides resizable virtual server instances in the cloud, forming the backbone of AWS compute infrastructure.',
      bulletPoints: [
        'Instance Types: t (general), c (compute), m (memory), r (memory-optimized), p/g (GPU), i (storage-optimized).',
        'AMIs: Amazon Machine Images represent pre-configured OS and application templates used to launch instances.',
        'Security Groups: Stateful virtual firewalls at the instance level. Inbound/outbound rules evaluated separately.',
        'Key Pairs: RSA/ED25519 public-private keys used for secure SSH/RDP access. AWS stores only the public key.',
        'Placement Groups: Cluster (low latency), Spread (fault isolation), or Partition (large distributed workloads like Hadoop).',
        'Spot Instances: Up to 90% cheaper than On-Demand but can be interrupted by AWS. Ideal for fault-tolerant batch jobs.',
      ],
      codeSnippet: '# Launch an EC2 instance with the CLI\naws ec2 run-instances \\\n  --image-id ami-0c55b159cbfafe1f0 \\\n  --instance-type t3.micro \\\n  --key-name MyKeyPair \\\n  --security-group-ids sg-903004f8 \\\n  --count 1',
    ),
    CheatsheetSection(
      title: 'S3 & Object Storage',
      content: 'Simple Storage Service (S3) is an object storage service designed for storing files, static website assets, and backups with 99.999999999% (11 9s) durability.',
      bulletPoints: [
        'Buckets & Objects: Files ("objects") stored inside globally unique "buckets". Each object has a key, value, metadata, and ACL.',
        'Storage Classes: Standard > Intelligent-Tiering > Standard-IA > One Zone-IA > Glacier Instant > Glacier Flexible > Glacier Deep Archive.',
        'Lifecycle Policies: Automatically transition objects between storage classes or delete them after set days.',
        'S3 Versioning: Keeps multiple versions of an object; each version has a unique ID. Protects from accidental deletion.',
        'Bucket Policies vs ACLs: Bucket policies are resource-based JSON IAM policies; ACLs grant per-object permissions to specific AWS accounts.',
        'Transfer Acceleration: Routes uploads via AWS CloudFront edge locations for faster global uploads.',
      ],
      codeSnippet: '# Upload a file and set storage class\naws s3 cp archive.tar.gz s3://my-bucket/backups/ \\\n  --storage-class STANDARD_IA\n\n# Sync a local folder to S3\naws s3 sync ./dist s3://my-website-bucket/ --delete',
    ),
    CheatsheetSection(
      title: 'VPC & Networking',
      content: 'Virtual Private Cloud (VPC) lets you provision a logically isolated section of the AWS cloud where you launch resources in a virtual network you define.',
      bulletPoints: [
        'Subnets: IP address sub-ranges within a VPC. Public subnets route to an Internet Gateway; private subnets do not.',
        'Internet Gateway (IGW): Enables bidirectional internet access for resources in public subnets.',
        'NAT Gateway: Lets private subnet instances initiate outbound internet connections while blocking inbound traffic.',
        'Route Tables: Control where network traffic is directed. Each subnet is associated with exactly one route table.',
        'VPC Peering: Direct network connection between two VPCs using private IPs. Non-transitive — peering A-B and B-C does not connect A-C.',
        'Security Groups vs NACLs: Security Groups are stateful and instance-level; NACLs are stateless and subnet-level.',
      ],
      codeSnippet: '# Create a VPC and public subnet\naws ec2 create-vpc --cidr-block 10.0.0.0/16\naws ec2 create-subnet \\\n  --vpc-id vpc-0abc1234 \\\n  --cidr-block 10.0.1.0/24 \\\n  --availability-zone us-east-1a',
    ),
    CheatsheetSection(
      title: 'IAM & Access Management',
      content: 'Identity and Access Management (IAM) is the global service controlling authentication and authorization for all AWS resources.',
      bulletPoints: [
        'Users: Long-lived identities for people or services. Should have access keys or console password, not both by default.',
        'Groups: Collections of IAM Users sharing a policy set. Cannot be nested.',
        'Roles: Temporary credentials for trusted entities (EC2 instances, Lambda, cross-account access). No static credentials.',
        'Policies: JSON documents containing Version, Statement, Effect (Allow/Deny), Action, and Resource fields.',
        'Permission Boundary: Advanced IAM feature setting the maximum permissions a user or role can have.',
        'MFA (Multi-Factor Authentication): Adds a second factor (TOTP app, hardware key) to console logins for security.',
      ],
      codeSnippet: '{\n  "Version": "2012-10-17",\n  "Statement": [\n    {\n      "Effect": "Allow",\n      "Action": ["s3:GetObject", "s3:PutObject"],\n      "Resource": "arn:aws:s3:::my-bucket/*",\n      "Condition": {\n        "StringEquals": {"aws:RequestedRegion": "us-east-1"}\n      }\n    }\n  ]\n}',
    ),
    CheatsheetSection(
      title: 'Lambda & Serverless',
      content: 'AWS Lambda runs code in response to events without provisioning or managing servers. It scales automatically from zero.',
      bulletPoints: [
        'Supported Runtimes: Node.js, Python, Java, Go, Ruby, .NET, and custom runtime via Layers.',
        'Event Sources: API Gateway, S3, DynamoDB Streams, SQS, SNS, EventBridge, CloudWatch Events.',
        'Execution Context: Lambda reuses the same execution environment across warm invocations (connection pools persist).',
        'Concurrency: Reserved concurrency limits simultaneous executions per function. Provisioned concurrency eliminates cold starts.',
        'Layers: Shared libraries and dependencies packaged separately, up to 250MB unzipped per function.',
        'Cold Start: First invocation delay (ms to seconds) for initializing the execution environment. Affects latency-sensitive apps.',
      ],
      codeSnippet: '# Deploy a Lambda function via CLI\naws lambda create-function \\\n  --function-name my-processor \\\n  --runtime python3.12 \\\n  --handler index.handler \\\n  --role arn:aws:iam::123456789012:role/lambda-role \\\n  --zip-file fileb://function.zip',
    ),
    CheatsheetSection(
      title: 'RDS & Database Services',
      content: 'AWS Relational Database Service (RDS) provides managed engines. DynamoDB handles NoSQL at any scale.',
      bulletPoints: [
        'RDS Engines: MySQL, PostgreSQL, MariaDB, Oracle, SQL Server, and Aurora (AWS-native compatible engine).',
        'Multi-AZ: RDS automatically provisions a standby replica in a different Availability Zone for failover (synchronous replication).',
        'Read Replicas: Asynchronous copies of the primary DB for read-scaling; can be promoted to standalone.',
        'Aurora: Up to 5x faster than MySQL, 3x faster than PostgreSQL. Storage auto-scales to 128TB. 6-way replication.',
        'DynamoDB: Key-value and document NoSQL database. Millisecond latency at any scale. On-demand or provisioned capacity modes.',
        'ElastiCache: Managed Redis or Memcached cache. Reduces DB load for frequently-read data (session stores, leaderboards).',
      ],
      codeSnippet: '# Create an RDS PostgreSQL instance\naws rds create-db-instance \\\n  --db-instance-identifier mydb \\\n  --db-instance-class db.t3.micro \\\n  --engine postgres \\\n  --master-username admin \\\n  --master-user-password secret123 \\\n  --allocated-storage 20',
    ),
    CheatsheetSection(
      title: 'CloudWatch & Monitoring',
      content: 'Amazon CloudWatch is the observability backbone of AWS, providing metrics, logs, alarms, and dashboards for all AWS services.',
      bulletPoints: [
        'Metrics: Numerical time-series data points from AWS services (CPU, NetworkIn, etc.). Published every 1-5 minutes by default.',
        'Alarms: Trigger actions (SNS notification, Auto Scaling, Lambda) when a metric breaches a threshold for N consecutive periods.',
        'Logs: Centralized log storage with structured query using CloudWatch Logs Insights. Supports log groups and streams.',
        'Dashboards: Customizable real-time visualizations of metrics across multiple services and accounts.',
        'Events / EventBridge: Route events from AWS services to targets like Lambda, SQS, or Step Functions.',
        'X-Ray: Distributed tracing service for debugging latency bottlenecks in microservice architectures.',
      ],
      codeSnippet: '# Create an alarm for high CPU usage\naws cloudwatch put-metric-alarm \\\n  --alarm-name "HighCPU" \\\n  --metric-name CPUUtilization \\\n  --namespace AWS/EC2 \\\n  --statistic Average \\\n  --period 300 \\\n  --threshold 80 \\\n  --comparison-operator GreaterThanThreshold \\\n  --evaluation-periods 2',
    ),
    CheatsheetSection(
      title: 'CloudFront & CDN',
      content: 'Amazon CloudFront is a global Content Delivery Network (CDN) that serves content from 400+ edge locations worldwide, reducing latency.',
      bulletPoints: [
        'Origins: Where CloudFront retrieves content — S3 buckets, ALBs, EC2, API Gateway, or any HTTP server.',
        'Cache Behaviors: Rules controlling which requests are cached, for how long, and how headers/cookies are forwarded to origin.',
        'TTL (Time-To-Live): Controls how long objects remain in edge caches. Min/Max/Default TTL configurable per behavior.',
        'OAC (Origin Access Control): Restricts S3 bucket access to only the CloudFront distribution (replaces legacy OAI).',
        'WAF Integration: AWS Web Application Firewall can be attached to a CloudFront distribution to filter malicious traffic at the edge.',
        'Invalidations: Force CloudFront to evict cached items before TTL expires. Charged per path (first 1000/month free).',
      ],
      codeSnippet: '# Invalidate a cached path in CloudFront\naws cloudfront create-invalidation \\\n  --distribution-id EDFDVBD6EXAMPLE \\\n  --paths "/index.html" "/assets/*"',
    ),
  ],
);
