import '../../models/cheatsheet.dart';

final Cheatsheet azureCheatsheet = Cheatsheet(
  category: 'Azure',
  summary: 'Microsoft Azure is a major enterprise cloud platform. This guide covers Virtual Machines, Blob Storage, Networking, Entra ID, Cosmos DB, AKS, Azure Functions, and Azure Monitor.',
  sections: [
    CheatsheetSection(
      title: 'Virtual Machines & Scaling',
      content: 'Azure Virtual Machines provide on-demand, high-performance virtual compute resources supporting Windows and Linux operating systems.',
      bulletPoints: [
        'Resource Groups: Logical containers holding related Azure resources. Deleting a group removes all contained resources.',
        'VM Sizes: Families by workload — B (burstable), D (general), E (memory), F (compute), N (GPU), H (HPC).',
        'Availability Sets: Group VMs across Fault Domains (power/network) and Update Domains to limit impact of failures and maintenance.',
        'Availability Zones: Physically separate datacenters in the same region. Deploy across 3 zones for maximum HA.',
        'Scale Sets (VMSS): Auto-scale group of identical VMs. Supports manual, scheduled, or metric-based scaling rules.',
        'Reserved VM Instances: Commit to 1 or 3 years for up to 72% discount over Pay-As-You-Go pricing.',
      ],
      codeSnippet: '# Create a VM and enable auto-shutdown\naz vm create \\\n  --resource-group learn-rg \\\n  --name webVM \\\n  --image Ubuntu22LTS \\\n  --size Standard_B2s \\\n  --admin-username azureuser \\\n  --generate-ssh-keys\n\n# Stop and deallocate VM to save costs\naz vm deallocate --resource-group learn-rg --name webVM',
    ),
    CheatsheetSection(
      title: 'Blob Storage & Storage Accounts',
      content: 'Azure Blob Storage is Microsoft\'s object storage solution for the cloud, optimized for storing massive amounts of unstructured data.',
      bulletPoints: [
        'Storage Account Types: General-purpose v2 (recommended), Blob Storage, Premium Block Blob (low latency), Premium File Shares.',
        'Blob Containers: Organize blobs within a storage account — similar to folders. Public access configurable per container.',
        'Blob Types: Block Blobs (files, images, video), Page Blobs (VHD disks, up to 8TB), Append Blobs (log files).',
        'Access Tiers: Hot (frequent access), Cool (infrequent, 30+ days), Cold (90+ days), Archive (180+ days, offline retrieval).',
        'Shared Access Signatures (SAS): Time-limited tokens granting access to specific storage resources without sharing account keys.',
        'Azure Data Lake Storage Gen2: Hierarchical namespace built on Blob Storage for big data analytics workloads.',
      ],
      codeSnippet: '# Upload a file to Azure Blob storage\naz storage blob upload \\\n  --account-name mystorageacct \\\n  --container-name logs \\\n  --name 2025-app.log \\\n  --file ./logs/app.log\n\n# Generate a SAS URL valid for 1 hour\naz storage blob generate-sas \\\n  --account-name mystorageacct \\\n  --container-name logs \\\n  --name 2025-app.log \\\n  --permissions r --expiry 2025-07-27T18:00:00Z',
    ),
    CheatsheetSection(
      title: 'Azure Networking & VNets',
      content: 'Azure Virtual Network (VNet) is the fundamental building block for your private network in Azure.',
      bulletPoints: [
        'VNet: Logically isolated network. Supports custom IP address ranges, subnets, route tables, and network security groups.',
        'Network Security Groups (NSGs): Stateful firewall rules (Allow/Deny by priority) applied to subnets or NICs.',
        'Azure Load Balancer: Layer 4 (TCP/UDP) load balancing. Basic (free, no SLA) vs Standard (zone-redundant, 99.99% SLA).',
        'Application Gateway: Layer 7 (HTTP/HTTPS) load balancer with URL-based routing, SSL termination, and WAF integration.',
        'VNet Peering: Connect VNets in the same or different regions. Traffic stays on Microsoft backbone — not the internet.',
        'Private Endpoints: Brings Azure PaaS services (Storage, SQL, Key Vault) inside your VNet using a private IP.',
      ],
      codeSnippet: '# Create a VNet with two subnets\naz network vnet create \\\n  --resource-group learn-rg \\\n  --name prod-vnet \\\n  --address-prefix 10.0.0.0/16 \\\n  --subnet-name frontend \\\n  --subnet-prefix 10.0.1.0/24',
    ),
    CheatsheetSection(
      title: 'Entra ID & Identity',
      content: 'Microsoft Entra ID (formerly Azure Active Directory) is a cloud-based identity and access management service used by millions of organizations.',
      bulletPoints: [
        'Tenant: A dedicated Entra ID instance for an organization. Each tenant has a unique tenant ID (GUID).',
        'Users and Groups: Users can be cloud-only or synced from on-premises AD. Groups can be assigned, dynamic, or M365 groups.',
        'App Registrations: Register apps to authenticate users via OAuth 2.0 / OpenID Connect. Exposes client ID and tenant ID.',
        'Managed Identities: System-assigned or user-assigned identity for Azure resources, eliminating credential management.',
        'Conditional Access: Enforce MFA, block legacy protocols, restrict by IP, device compliance, or user risk level.',
        'RBAC (Role-Based Access Control): Azure RBAC controls access to Azure resources. Distinct from Entra ID app permissions.',
      ],
      codeSnippet: '# Assign RBAC role to a user on a resource group\naz role assignment create \\\n  --assignee user@example.com \\\n  --role "Contributor" \\\n  --resource-group learn-rg\n\n# List role assignments\naz role assignment list \\\n  --resource-group learn-rg --output table',
    ),
    CheatsheetSection(
      title: 'Cosmos DB & Relational Data',
      content: 'Azure offers globally distributed database options. Cosmos DB serves NoSQL at planetary scale, while Azure SQL Database handles relational workloads.',
      bulletPoints: [
        'Cosmos DB: Multi-model, multi-region NoSQL database with single-digit ms latency at any scale. 99.999% SLA.',
        'Cosmos DB APIs: Core (SQL), MongoDB, Cassandra, Gremlin (graph), Table API. All backed by the same engine.',
        'Consistency Levels: Strong, Bounded Staleness, Session (default), Consistent Prefix, Eventual — trade-off between consistency and latency.',
        'Azure SQL Database: Fully managed SQL Server engine. Serverless tier auto-pauses when idle to save costs.',
        'Azure Database for PostgreSQL / MySQL: Managed open-source databases with automated patching and backups.',
        'Geo-Replication: Both Cosmos DB and Azure SQL support active geo-replication for disaster recovery and read distribution.',
      ],
      codeSnippet: '# Create a Cosmos DB account with SQL API\naz cosmosdb create \\\n  --name my-cosmos-account \\\n  --resource-group learn-rg \\\n  --kind GlobalDocumentDB \\\n  --locations regionName=EastUS failoverPriority=0 \\\n  --default-consistency-level Session',
    ),
    CheatsheetSection(
      title: 'AKS & Container Services',
      content: 'Azure Kubernetes Service (AKS) simplifies Kubernetes cluster management. Azure Container Apps offers a fully managed serverless Kubernetes experience.',
      bulletPoints: [
        'AKS: Managed Kubernetes control plane (free). You pay only for worker node VMs, storage, and networking.',
        'Node Pools: System node pools run core Kubernetes workloads; User node pools for application pods. Taints/tolerations control placement.',
        'Azure CNI: Advanced networking plugin for AKS assigning real VNet IPs to Pods (better integration with Azure services).',
        'Workload Identity: Pods authenticate to Azure services via federated identity — no secrets needed in the cluster.',
        'Azure Container Registry (ACR): Private Docker container registry integrated with AKS for secure image pulls.',
        'Container Apps: Serverless Kubernetes powered by KEDA. Scale-to-zero on HTTP, Pub/Sub events, or custom metrics.',
      ],
      codeSnippet: '# Create an AKS cluster\naz aks create \\\n  --resource-group learn-rg \\\n  --name prod-aks \\\n  --node-count 3 \\\n  --node-vm-size Standard_DS2_v2 \\\n  --enable-managed-identity \\\n  --generate-ssh-keys\n\n# Get credentials for kubectl\naz aks get-credentials \\\n  --resource-group learn-rg --name prod-aks',
    ),
    CheatsheetSection(
      title: 'Azure Functions & Serverless',
      content: 'Azure Functions is an event-driven serverless compute service. Durable Functions adds state and orchestration capabilities.',
      bulletPoints: [
        'Triggers: HTTP, Timer, Blob Storage, Queue Storage, Service Bus, Event Hub, Cosmos DB, and more.',
        'Hosting Plans: Consumption (auto-scale, pay-per-execution), Premium (pre-warmed instances, VNet integration), Dedicated (always-on App Service plan).',
        'Durable Functions: Extension for stateful workflows (Function Chaining, Fan-Out/Fan-In, Human Interaction patterns).',
        'Bindings: Declarative way to connect functions to services without writing connection boilerplate (input/output bindings).',
        'Cold Starts: Consumption plan may have startup latency. Premium plan eliminates this with always-ready instances.',
        'Languages: C#, JavaScript/TypeScript, Python, Java, PowerShell, and custom handlers for any language.',
      ],
      codeSnippet: '# Deploy a Python Azure Function app\nfunc azure functionapp publish my-func-app \\\n  --python\n\n# Local development\nfunc start  # Run functions locally\nfunc new --name ProcessOrder --template "HTTP trigger"',
    ),
    CheatsheetSection(
      title: 'Azure Monitor & Observability',
      content: 'Azure Monitor collects, analyzes, and acts on telemetry from Azure and on-premises environments.',
      bulletPoints: [
        'Metrics: Numerical time-series data from Azure resources. Stored for 93 days. Viewable in Metrics Explorer.',
        'Log Analytics Workspace: Central repository for log data. Query using Kusto Query Language (KQL).',
        'Application Insights: APM service for web applications. Tracks requests, dependencies, exceptions, and custom events.',
        'Alerts: Trigger action groups (email, SMS, Logic App, webhook) when metrics or log query results breach thresholds.',
        'Diagnostic Settings: Route resource logs and metrics to Log Analytics, Storage Account, or Event Hub.',
        'Azure Dashboard: Custom pin views of metrics, charts, and KQL query results for at-a-glance observability.',
      ],
      codeSnippet: '// KQL query in Log Analytics — find failed requests\nrequests\n| where timestamp > ago(24h)\n| where success == false\n| summarize Count = count() by bin(timestamp, 1h), resultCode\n| order by timestamp desc',
    ),
  ],
);
